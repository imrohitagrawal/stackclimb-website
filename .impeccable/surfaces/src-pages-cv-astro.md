---
version: 1
slug: "src-pages-cv-astro"
primary_target: "src/pages/cv.astro"
related_targets: []
---

# CV — surface brief

**DRAFT — P5 groundwork, not owner-approved.** Visitor mode below is a proposal, not a
pinned decision. P5's own fourth item — "decide whether `/cv`'s Independent Systems stays a
disclosure at all" — is a self-description/product decision this brief cannot resolve; it is
recorded below as still open, with fresh evidence for the owner to weigh.

Scope: `/cv` (src/pages/cv.astro). Visitor mode (proposed): **Take away** — the one page on the
site built to be printed, forwarded, or pasted into an ATS (D31, P-22), not read in the browser
as the primary experience.

Audience & job (proposed): a recruiter or hiring manager who has already decided to move
forward and needs the CV as an artifact — to forward internally, print, or paste. Job: leave
with a complete, credible, take-away-able document.

Action (proposed): download (build-time PDF, queued per P-22, not yet built) and copy (done,
P-22/D152) — no persuasion job, no new action beyond taking the document away.

Proof: employer ledger (approximate, attributed, P-16/P-21), six independent systems each with
a Gate/Rule statement and Visit/Evidence links (P1 fixed — the PDF and clipboard exports used
to drop this section entirely).

Chosen direction: same portrait-plate world for the screen render; a fully inverted print
stylesheet for paper (D31's own reasoning — bone-on-black would print blank).

**Open, needs the owner — P5's own item 4, with fresh evidence:**
- Re-checked 2026-08-30, opening the NarraTwin card live: it now prints the full context —
  *"Its own release-readiness review reads No-Go, so it is not deployed. It is shown anyway,
  because the gate holding is the point."* — not a bare failure line. P1's fix (making the PDF
  and clipboard exports actually carry this text, where they used to drop it) has already
  addressed the SPECIFIC complaint D153 raised (a No-Go reading as "a bare failure line with
  nothing around it explaining what NarraTwin is").
- What P1 did NOT resolve, and what's actually still open: whether NarraTwin (and, by the same
  question, EvalAxis and Aegis Contracts, the other two non-deployed entries) belong in a
  section titled "Independent Systems" on the document a recruiter takes away, or whether a
  CV — as distinct from the home page, which has more room to contextualize a disclosure — is
  the right place for an undeployed project at all. This is the reserved, self-description
  question P5 names; it needs the owner's judgment on what the CV should claim to be, not
  another round of layout or copy fixes.
