# Status

The ledger. What is decided, what is open, and what was rejected so it does not come back.

Updated in the same change that alters any of it. If this file and the chat disagree, this file
is wrong and must be fixed — chat is not a record.

Last updated: 2026-08-11 (D56 — review findings on D55 closed: motion test coverage added,
plan-status docs corrected, the hero whitespace claim corrected, all on branch
`hero-animations-wordmark`, not yet merged as this line was written)

---

## Where the project is

| | |
|---|---|
| Repo | `github.com/imrohitagrawal/stackclimb-website` · **PUBLIC as of 08-09 (D53).** Recreated 08-09 (DEF-50) before the flip — old repo lives on privately as `stackclimb-website-archived-20260809`, current repo's history was rewritten to strip `docs/brand/candidates` and started fresh with zero PR refs |
| Branch | `main` — run `git rev-list --count HEAD` for the count; do not hard-code it here |
| Site | Home page built — 7 plates, six record cards (CiteVyn carries two — its own run plus the relocated refusal case) each quoting a named file at a named commit, ruled caption strips. Nav wordmark is the owner's real "StackClimb" image, visible at every width |
| Deployed | **Manually, and it drifts.** `wrangler pages project list` reports `Git Provider: No` — nothing deploys on push; every deploy is a hand-run `wrangler pages deploy`. Measured 08-09 overnight: production was serving `7ec8c24` while `main` stood **six commits ahead**. Check with `npx wrangler pages deployment list --project-name stackclimb` and compare the CSS hash, never assume. `stackclimb.com` and `www` both serve HTTP 200 from Cloudflare Pages project `stackclimb`, production branch `main`, cert CN=stackclimb.com valid to 24 Oct 2026. Two CNAMEs added by the owner; the three Fly subdomains were verified unchanged before and after |
| Tests | **104 pass, 0 fail, 2 skipped** (08-11 — the skips are the coarse-pointer gate on the desktop project, by design), plus `tests/visual-baselines.spec.js`, which fails on macOS by design against Linux-rendered baselines (see D55) and is excluded from that count. Gates that fail loudly: plate seams (`boundary-check.mjs`), nav pixels incl. the open menu (`nav-contrast.mjs`), PII (`no-pii.mjs`), file budget (`file-budget.mjs`), nav reachability, palette ladder, focus visibility in pixels, chrome a11y, tap targets. The suite BUILDS before it tests (DEF-11) and refuses to reuse a stale server |
| CI | **`.github/workflows/gates.yml`, added 08-09 — the first mechanical enforcement this repo has had.** Two jobs: secrets-and-PII (gitleaks with `fetch-depth: 0`, `.env` untracked, inbox untracked, `tests/no-pii.mjs`) and build-and-test (build **before** test per DEF-11, PII scan of the *build*, playwright, plate-seam contrast). Green on its first **push** run, `31276450288`, both jobs, 08-09. **That is a narrower claim than this row used to make** — see DEF-39. The workflow also triggers on `pull_request`, and the first time a PR actually ran it (`31278058454`) the secrets job failed in 8 seconds on a 403. A push run is not evidence the PR path works, and the PR path is the one branch protection gates on. Fixed 08-09 overnight. Branch protection still cannot require it until the repo is public or on Pro |

---

## Decided

| # | Decision | Why | Date |
|---|---|---|---|
| D59 | **The `/impeccable critique` of the mockup scored 22/36 and found the chosen World A breaks four rules `DESIGN.md` bans by name — fixed by ONE artefact panel per band, not one per card, which is what the rule actually says** | Dual-agent run (design review + detector/browser evidence, isolated and parallel). **The P0: the Lit-Surface Rule was abandoned.** `--lit` was declared and used only as a text colour; every proof was *prose about* an artefact with the artefact's filename demoted to 10px grey measuring **4.33:1**, below AA. `DESIGN.md:253` is unambiguous — *"A page that promises 'AI that shows its work' cannot show a wireframe where the work belongs."* **Correction to my own first fix:** I proposed an artefact panel per card; re-reading the rule, it says **"Exactly one near-white surface per plate"** — one, not one per card. Zero violates it; six violates it equally. **Decided: one artefact panel per band, holding the single strongest real output (CiteVyn's recorded refusal, `golden_report.json` at `df8cfc3`), with the other five as compact prose cards.** This also fixes the hierarchy finding — *"the cards are six equal arguments; one of them is the argument"* — without adding to a mobile section already measured at **4,136 CSS px, 4.9 iPhone viewports**, against the One-Plate-One-Viewport Rule. Each `/projects/<name>` page gets its own single panel. Other rules broken and being fixed: eyebrow labels, status badges and the white-grid card gallery, all banned by name at `DESIGN.md:327`. **Measured evidence that independently confirmed D58's World A choice:** World B's accent `#b0682f` **never reaches 4.5:1 on any World B background** (4.20–4.31:1 across every use, including all links); World B failed AA on 5 of 11 sampled elements against World A's 1. Detector: exit 2, 25 findings (18 `design-system-color`, 5 `design-system-font-size`, 1 `side-tab`, 1 `em-dash-overuse` at 35 em-dashes). axe: 67 `color-contrast` nodes, 14 in World A. **15 of 15 tap targets measured 15–16px tall against a 44px minimum.** Snapshot: `.impeccable/critique/2026-08-11T10-10-01Z__mockup-index-html.md` | 08-11 |
| D60 | **StackClimb is defined on the site, in the owner's words, because a cold reader can otherwise read it as a former employer** | Critique finding: "Independent StackClimb systems" appears as an established term in a column heading, but StackClimb is the domain name. A recruiter arriving from LinkedIn may reasonably read it as a company he worked at — which **inverts the meaning of "independent" into its exact opposite**, the one distinction the two-ledger section exists to draw. Owner's canonical line, already designed into the footer: *"The engineering portfolio and product studio of Rohit Agrawal. Independent projects are built outside any employer. Employer outcomes are attributed to their employer and marked approximate."* It does two jobs at once — defines the name and states the two-ledger discipline. Ships in the footer as designed, **and** its first clause appears where a cold reader first meets the word. **"Product studio" was flagged by me as a possible claim and confirmed twice by the owner as his own descriptor** — recorded as decided, not re-raised: it is a self-description, not a metric. **Also decided:** the `Being built` group is ordered **Aegis → EvalAxis → NarraTwin**, so the band closes on NarraTwin's No-Go. Peak-end says the last card carries disproportionate weight, and a gate that *held* is a disclosure — the section ends on the thesis rather than on an absence (Aegis's signature proof is a question, not a proof, and it was previously last) | 08-11 |
| D61 | **`DESIGN.md`'s Lit-Surface Rule amended: one near-white artefact panel per SYSTEM, not per plate — four systems get a panel, two get none** | Owner: *"I want one artefact panel for every project holding my strongest piece of real output."* **Owner's ruling, which settles it: a project card counts as a plate.** The old wording — "exactly one near-white surface per plate" — was written when a plate was a full-viewport section holding one system; D57's restructure puts six systems in one section, so read literally it capped the section at one panel and left five systems describing their evidence in prose instead of showing it. The unit the rule protects is the *system*, not the section. **The ban it exists to enforce is unchanged and now explicit for the empty case: a system with no artefact yet gets no panel, never a placeholder.** So EvalAxis (private) and Aegis Contracts (nothing built) get prose cards, not empty panels. `Artefact.astro` already carries a `compact` prop, so this needs no new component. **Panels use live captures and real operational documents, never test-suite output — a finding that changed the plan:** CiteVyn's golden suite is **fixture-backed**, `claude_api_001` returning *"Per the cited source [1], this is documented behavior"* citing **`https://docs.example.com/claude-api`**, a placeholder domain. The 52/52 pass rate is a real gate and safe to state; printing an *answered* case's content would put a fake citation URL on a site whose whole argument is inspectable evidence. **Correction to my own escalation:** I first implied the shipped panel was at risk — it is not. The refusal case `claude_api_006` carries `citations: []`, `domain: unsupported`, `confidence: none` and the real production policy string; no fabricated URL appears in it. The risk applied only to the alternative I was proposing. What remains is the owner's original objection, and it stands: "What is the capital of France?" is genuinely the recorded question but reads as a toy. It cannot be improved by picking another case — **all seven `no_answer` cases return one identical string** — so a live capture against the running app is the only route to a stronger one, scheduled as a refinement | 08-11 |
| D74 | **Four claims the site made about itself were false, and one of them was a claim another repo wrote an ADR to kill. All four fixed; the grid that refuted its own lede now agrees with it** | Found by the D72 audit, fixed here, and every one verified against the *source repository* rather than against this file. **(1) Quorum said the four models critique one another.** `quorum-ai/docs/adr/0032` exists precisely to end that claim: `debate.py`'s `_call_debate_model` is the **only** debate dispatch site, it sends one separate moderator model a transcript of all four answers, twice, and *"the four answer models never see each other's answers — there is no code path on which they could."* Quorum corrected the claim across six of its own files on 08-11; **this site went on serving it**, on the plate body and repeated in the `#overview` card, which is the harder failure — the correction was made and did not travel. Both now read that a fifth model, a separate moderator, reads all four and critiques them twice. **Round 2 is the moderator re-reading its own round-1 critique**, so "twice" is stated without implying a second party. **(2) The hero counted `Systems built 6` while the private plate said two are still being built.** Now `4 of 6`. The neighbouring cell read `Release gates 4 of 6` — a *different* four (SaafSaans is built but is not a gate; EvalAxis is a gate but is not built), so two identical strings invited the reader to merge two disjoint sets. It now reads `4` with no denominator, and the denominator lives where the sets are listed. **(3) The `#overview` lede says four of six are gates; the grid beneath printed `GATE` on all six.** `docs/positioning-phase0.md:77` names exactly four — CiteVyn, NarraTwin, EvalAxis, Quorum. SaafSaans' entry is a **disclosure rule**, which stops nothing shipping, and now prints `RULE`. Aegis had *"Too early to state one"* under a `GATE` label and now prints nothing, for the same reason the figure column already refuses an em dash: an absence wearing a label is the thing that column was fixed for once already. **(4) SaafSaans claimed a `sample` mode the product deleted.** `saafsaans/web/presenters.py:570-572`: the chip *"used to say SAMPLE, which was accurate while a sample existed… it no longer serves one, so there is no sample to name."* The live states are **LIVE / CACHED / NO READING**; the site named two of three wrongly. **Two orphans removed:** `Artefact.astro` (186 lines, imported by nothing, with `Layout.astro:121` and `motion-toggle.js:13` still naming it a live consumer — both comments corrected) and `saafsaans-hi.webp`, which `Shot.astro`'s eager glob emitted into `dist/_astro/` at **41.9 KB per visitor** for zero references. **Two defects found by looking at the render, neither by any gate.** Fixing the Quorum body made it restate the engineering line word for word, so the plate said the same mechanism twice back to back — the engineering line now starts at engineering. And `.proof`'s `margin: 0 0 1rem` shorthand beat `.plate-copy p + p` on specificity, so **the proof line butted onto the paragraph above and read as its last sentence, on Quorum and SaafSaans both**. No gate asserts vertical rhythm; the screenshot did. **Measured:** build clean, file budget 60 files green, seam gate worst 7.56:1 AA, nav contrast worst 6.26:1, PII 113 files clean, no horizontal scroll at 390 or 1440, **124 Playwright tests passing**. The 6 visual-baseline failures are against gitignored local `-darwin` files; the diff image shows the delta is **exactly the two hero strip values and nothing else**. Tracked `-linux` baselines regenerate on the runner (D69) | 08-12 |
| D73 | **Approach C is written and one secret away: `gates.yml` deploys and then verifies production. The post-deploy gate gained the settle wait and N-of-M rule correction 281 required** | **The deploy job** `needs` both gate jobs and is gated `if: push && ref == main`, so a red gate cannot reach production and a PR build never touches it. It builds, runs `wrangler pages deploy`, waits 20s, then runs `tests/post-deploy.mjs` against the live origin — because **the deploy is not the check**: wrangler printed `Success! Uploaded 13 files (20 already uploaded)` on the run that shipped a 404. **Blocked on one secret, `CLOUDFLARE_API_TOKEN`.** Until it exists the job **skips loudly** — a `::warning::` plus a run-summary line saying production was not updated — rather than failing. A skipped deploy that looks like a passed deploy is the precise failure the job exists to prevent, so the absence is made visible instead. **Separately, `tests/post-deploy.mjs` was a bare single-shot fetch — the exact shape correction row 281 forbids** after a seam gate went red-then-green three times on 08-09 with the cause never established. It now states its rule up front and enforces it: a settle wait before the first fetch, and an asset is FAILED only if it fails **all 3** attempts spaced 1.5s apart. A recovery inside the rule is reported as **flapping** and never silently swallowed, because unexplained flapping is a cause, not noise. Both directions proved by self-test: **one transient 503 recovers and reports as flapping (0 problems, 1 flapped); a persistent failure still fails after all attempts.** Eight self-test assertions now pass | 08-12 |
| D72 | **A ledger audit found nine rows wrong, two of them measured numbers — and the audit happened only because a plan overwrite silently dropped half the register** | Three read-only agents were told **not to trust this file** and to verify every row by running a command. **The trigger:** the working plan document had been overwritten once, replacing a full pending register with a five-package plan carrying none. M6, M9, M4, branch protection, `/cv` ResumeSkills, P-12, W-20, DEF-19/21/24/49 and D30 fell out in one stroke, and the `colorize` package was lost entirely. They survived only because this file held them — **luck in the design, not diligence**, and exactly the carry-forward audit `AGENTS.md` requires when consolidating. **The two that mattered most were numbers.** O-30SEC claimed the name renders at 18.24px against a 96px headline. Measured with `getComputedStyle` on the served build: **name 31.68px, h1 59.04px** — 1 : 1.86, not 1 : 5.3. **18.24px is the lede paragraph**, and **96px is a `clamp()` value deleted long ago**, with the replacement and an explanatory comment sitting at `global.css:289`. A row wrong by 1.74× on one figure and quoting a deleted value for the other would have sent the next reader to fix a problem that no longer has that shape. **Also refuted:** O-30SEC's "five job titles" (one ships), O-SKILLGAP (six positioning skills installed — recorded by this file's own D34), M2 (14 of 14 present), I-2 (public since 08-09, and `STATUS.md` had already struck O2 for the identical staleness), W-6 (tracked `.githooks/pre-commit` + CI + enforced protection all exist), W-18 (`AGENTS.md:341` has the rule it says is missing), D62's residual (D66 amended it the same day), D69's "and from disk" (54 darwin files are back, harmlessly). **M6 struck as won't-do:** its observation is true but **DEF-3 records preloading killed on measurement** — desktop LCP +44ms, mobile +96ms, 7-run medians. The two have contradicted each other in this file since 08-09; the measurement wins. **M5 marked unauditable:** striking it replaced its item text with the word "Done", so no claim survives to test. When striking a row, strike the status and keep the item. **I-5's notice rewritten:** every row of the approach-C exit table now reads done, so the debt is no longer *blocked*, merely *not migrated* — a stronger and more actionable statement | 08-11 |
| D71 | **A deploy printed `Success` and shipped a 404. Branch protection is now on and tested, and `tests/post-deploy.mjs` is the first check in this repo that looks at production** | **The defect:** `wrangler pages deploy` reported *"Success! Uploaded 13 files (20 already uploaded)"* while `citevyn-demo.B8TdRMpf.webp` returned **404** on `stackclimb.com`. Cloudflare's content-hash dedup claimed to hold a blob it did not, and wrangler never reads back what it skips, so a wrong answer from the server is indistinguishable from a right one. A second deploy fixed it — 200 at 30,430 bytes. **Why nothing caught it, established by reading the workflows rather than guessing:** every gate runs against a local `astro preview` of a freshly built `dist/`, and the file was *correct* in `dist/`. The failure was in the upload, a step CI does not perform. **Zero deploy steps exist in any workflow, and nothing anywhere fetched production** — the only mention of `stackclimb.com` outside `astro.config.mjs` was a comment about canonical URLs. The page returned 200 the whole time; only fetching each referenced asset found it. **Two fixes, both proved rather than assumed. (1) Branch protection**, closing the last open row of the approach-C exit table: both gate jobs required, strict up-to-date, force-pushes and deletions blocked, conversation resolution required. **It was tested by trying to evade it** — a direct push to `main` was *accepted* while `enforce_admins` was false, and rejected with `GH006` once it was enabled. A gate that has never refused anything has proven nothing, and this one refused its author. One artefact of that test survives in history: empty commit `0cceb01`, which could not be removed because force-pushes are now blocked — the protection working as intended. **(2) `tests/post-deploy.mjs`**, with `--self-test` that plants each failure shape against a fixture server: it catches a 404 asset on a 200 page (the exact 08-11 defect), catches production serving an older build than `dist/`, counts the assets it checked so a checker that finds zero cannot pass everything, and stays silent on a healthy site. Green against production: 11 assets, build stamp matched. Added as **Definition of Done item 7** and `npm run post-deploy`. **Deliberately not put on a schedule.** `STATUS.md:266` already records that a post-deploy gate going red one run in three is worse than none, and with manual deploys a scheduled freshness check would fire constantly. It runs after a deploy, by the person who deployed | 08-11 |
| D70 | **The panels are ported into the real site as components; a review of the port caught six regressions, including a rule I cited in code but never wrote down** | The prototype lived in a git-ignored `mockup/`, so the panel work would have vanished with the directory while only the images and evidence survived. Ported as `src/data/projects.js` (content), `src/components/figures/Shot.astro` (the panel), `src/styles/shot.css` — and `index.astro` **shrank 480 → 420 lines**, so the port bought headroom under its 484 ceiling rather than spending it. **What the review caught, all fixed: (1)** `.proof` shipped with **zero CSS** — the measured-outcome line rendered byte-identical to body copy and its one ochre word came out as plain italic, so the panel's whole emphasis was silently lost. **(2)** `Shot.astro` and `shot.css` both cite **D66** as the authority for the screenshot carve-out, and **D66 had no row in this ledger** — the code depended on a decision that existed only in `DESIGN.md`. Recorded now as D66 below. **(3)** NarraTwin shipped `crop: null` and rendered at **19%** on a 390px phone, the exact failure its own component comment says the crop exists to prevent; SaafSaans' crop rendered **270×20px**, unreadable. Both re-cut — and NarraTwin's new crop is *better* than its full capture, because it frames the disclosure bar (`SIMULATED HOST CONTEXT`, the consent checkbox, `External web disabled by policy`) rather than a workflow table. **(4)** Quorum's real production readiness review — a Go dated 21 June and the No-Go it superseded five days earlier, both kept in the file — was **dropped and replaced by a SAMPLE fixture capture**. On a site arguing for real artefacts, trading a dated real decision for labelled canned data is a regression; restored in prose. **(5)** `19 live · 2 known gaps` had no evidence line; `saafsaans.md` now records that two of 21 `FEED_MAP` entries are `None` because WAQI carries no station for them, so quoting **21** counts absences as coverage. **(6)** NarraTwin's commit stamp was dropped from its strip, which is what makes the count checkable. **Partly refuted:** the review reported EvalAxis and Aegis losing their caption strips — they are on the untouched private plate at `index.astro:337`, grouped differently in the prototype. Their verified `13,769 lines · 388 tests` is still absent from the site, which is an opportunity, not a regression. **Measured after: build clean, file budget 58 files green, axe 0 violations at 1440 and 390, no horizontal scroll, all 8 images content-hashed into `dist/_astro/` (the DEF-24 trap avoided), both `SAMPLE` captions shipping, and zero occurrences of `628`, `MTTD` or `capital of France` in the output.** **Baselines regenerated and CI is green.** `workflow_dispatch` run `31510254419` produced them on the ubuntu-latest/chromium image, downloaded as an artifact and committed unchanged — never hand-made on a laptop (D69). **36 files changed, every one a `plate-*` baseline; zero `nav-*` moved.** That is the signature this change should have: the plates grew taller to hold the panels, the nav did not shift at all, and a changed nav baseline would have meant an unintended regression. Push run `31510575888` then passed the full suite on the runner. **One limitation stated rather than papered over: Codex produced no output in 25 minutes and was killed during session cleanup, so this round had a single review lens, not the cross-model pair I committed to.** | 08-11 |
| D66 | **`DESIGN.md`'s "no photographic texture" ban carved out for artefact panels** | The ban reads *"no second accent, no gradient, no photographic texture; the grain layer is the only material."* Taken literally it forbids the one thing the Lit-Surface Rule demands — *"actual system output at a size you can read"* — because a capture of the running system **is** a photograph of it. The rule that admits it is stronger than the rule that bans it, so the ban now carries one narrow carve-out: **only inside an artefact panel, only that system's own output, never decoration, never a background, never stock photography or a person.** Every such image carries a provenance caption naming its source, and anything that is not a live capture says so there. **This row exists because the code cited D66 before the ledger did** — `Shot.astro:6` and `shot.css:1` both name it, and a port review found the register had no such entry. A decision the code depends on that lives only in a design doc is exactly the drift this file exists to prevent | 08-11 |
| D69 | **I committed 54 laptop-generated visual baselines against the rule that forbids exactly that, and removed them. `.gitignore` now stops it recurring** | `git add -A` after a local `npx playwright test` swept up every `-darwin` snapshot Playwright had just written. `tests/visual-baselines.spec.js:18-21` is unambiguous: *"Baselines are NOT hand-generated on a laptop: they are produced by a CI run on the same ubuntu-latest/chromium image gates.yml uses… and only that run's artifacts are committed."* Shipped in `35afbf0`. **Blast radius, measured rather than assumed:** CI runs Linux and resolves `-linux` baselines, so no gate was broken and the 54 committed `-linux` files are untouched. The damage is subtler — a macOS contributor would then diff against *my laptop's* rendering, so real drift could pass locally while the CI baseline says otherwise, and 54 binary files of dead weight ride along. Removed from the index; the tracked set is back to **54 linux, 0 darwin**. **Correction 08-11: the "and from disk" half of this sentence is no longer true** — a local `npx playwright test` regenerates all 54 `-darwin` files, and 54 sit on disk now. Harmless, because `.gitignore` catches them and `git ls-files` shows 0 tracked, which is the half that matters. Recorded because a row claiming more than it delivers is the defect this session kept finding. **Prevented rather than just fixed:** `.gitignore` now excludes `tests/visual-baselines.spec.js-snapshots/*-darwin.png`, so the next `git add -A` cannot repeat it. **Found by auditing my own merge**, not by a gate — no check exists that fails when a laptop baseline is committed, which is itself worth knowing | 08-11 |
| D68 | **Two review rounds on the rebuilt artefact panels found the same defect class in all four components — fixture data presented as measurement — and the circuit breaker was honoured** | Round 1: three parallel lenses (truth, design, technical). Round 2: `codex exec --sandbox read-only`, a **different model family**, because `AGENTS.md` records that a subagent is context isolation and not model-weight decorrelation. **The breaker fired on rule 2 — the same class of defect in more than two components** — and implementation stopped for the owner rather than continuing to patch. **Diagnosis, per the rule that says name the miss before fixing: an understanding miss.** Every claim sentence had been compressed from a *research subagent's summary* of the code rather than from the code, so each one overstated in the same direction. The fix was to re-read every mechanism at source and rewrite, not to patch sentences. **What the reviews caught, all now corrected:** Quorum's `$0.188 actual · 41.2s` were **hardcoded fixture values** (`e2e/fixtures/golden-run.ts:381,390`) presented in a cell labelled *actual*, with `openrouter_live_execution_enabled: bool = False` by default and the PNG untracked at the named commit — the single worst thing nearly shipped, and it had already reached the ledger in `1ba1249`. CiteVyn's *"every index carries the provider, model and dimension"* was false: `hybrid.py:296-302` documents **three allow-paths**, including unstamped legacy indexes let through on purpose. SaafSaans' EPA claim inverted the truth — `risk.py:77-78` says *"EPA does not publish this mapping — it is our reading"* — and the page claimed a `SAMPLE` mode `presenters.py:571` records as **deleted**. NarraTwin's `BILLABLE_UNKNOWN` sentence was true of `GoogleGeminiTTSProvider` and **false of `ElevenLabsTTSProvider` in the same file**, which retries a timeout with `sleep(); continue` and no ledger; the panel now discloses that gap in its own copy. Quorum's *"two rounds of critique"* read as models critiquing each other, which ADR-0032 exists to prevent. **Round 2 then found the same class again:** `p95 26.2 ms` came from `build/gates/perf-percentiles.json`, stamped `run_id: "local"`, `sha: "local"` — **one laptop run** — while `tests/perf/test_workflow_latency_percentiles.py:28-40` records **ten** runs at **p95 42.2–82.3 ms**. The flattering sample from the least representative run. No latency figure ships, and *latency* is now in that panel's `Not claimed`. Mutation 88.7% also dropped: a partial false positive as raised, but the same test shows CI **declines to compare** it off-platform, and a figure CI will not compare is not one the site should quote. **Refuted, not fixed:** the design lens argued the NarraTwin panel must go under the no-placeholder rule — overruled by the owner, recorded as D67 rather than silently broken. **Measured after:** detector 0 anti-patterns, PII gate green over 108 files, mobile crops added via `<picture>` so each artefact is legible at 390px where full captures rendered at 15–17% scale | 08-11 |
| D67 | **`DESIGN.md`'s no-placeholder clause amended by the owner, over my objection** | The Lit-Surface Rule said a system with no artefact gets **no panel, never a placeholder**. A design review invoked it to remove the NarraTwin panel entirely. **Owner's decision: keep the image, label it `SAMPLE`.** My objection, recorded because it was made and overruled rather than dropped: the site's argument is that its evidence is inspectable, and a design is not evidence. The owner's position, which stands: a labelled design shows what the product does without claiming it runs, and this one carries its own disclosures on its face — `SIMULATED HOST CONTEXT`, `Local mock · no external provider calls, real media, or cloned identity`, `Verified sources · pending`. The rule now carries a narrow exception that does not travel: an interface **the owner built** for **that system**, captioned `SAMPLE` and saying in words it is not a running capture. Never stock art, never a competitor's UI, never unlabelled. Recorded in `DESIGN.md` inline, because a rule the pixels quietly break is drift | 08-11 |
| D65 | **M8 closed: all five evidence files written, every number re-derived at HEAD — and the audit refuted four figures I was about to ship, two of them mine** | `docs/evidence/projects/` now holds `citevyn.md`, `quorum-ai.md`, `saafsaans.md`, `narratwin.md`, `private.md`, each carrying the command and commit SHA behind every number. **Four refutations, recorded in the evidence README's mistakes list so the shape is not repeated:** (1) **"CiteVyn hit-rate 1.0 over 54 cases"** — 54 is the *judge* count; hit-rate 1.0 is over **26 answerable** cases, so the claim more than doubled the scope of a real result. (2) **"CiteVyn zero refusal leaks"** — `retrieval.refusal_leaks` is **5** of 19; only the *answer* layer is clean at `refusal_leaks_judged: 0`. (3) **"NarraTwin answerRelevancy 0.903, contextRecall 0.75"** — the committed `docs/EVAL_REPORT.md` records **1.0** for both; the lower figures came from a research subagent reading a JSON in a worktree since deleted, most likely a different newer run, now unrecoverable. (4) **"Aegis Contracts has no repository"** — mine, from 08-11: it exists and is private, merely not cloned, and *"not cloned"* is a different fact from *"does not exist"*. **The methodological finding, which the owner forced:** my first draft of the prompt to the NarraTwin session quoted the expected values back to it. He stopped it — asking a session to confirm numbers you have handed it invites it to reproduce them rather than report what it found, the same pass-filtered-generator failure this repo already measured on tests. The corrected prompt established existence, source and verification method before asking for anything, and withheld every figure. It came back with the refutation above. **NarraTwin's Lighthouse 100/100/100 is now `UNVERIFIED` and barred from the site** — the harness exists but no result is committed anywhere, `git log --all` on the path returns nothing, and Lighthouse is non-deterministic with no measured variance on record. Its eval figures may ship only with their commit stamp and the *"predates current HEAD"* caveat attached. **Also corrected on the live site:** SaafSaans' artefact card read 628 test functions / 32 files / 161 commits at `5e5037a`; at `667397a` it is **771 / 37 / 236**. Those figures had gone stale in three days, and the card's own source line now says so | 08-11 |
| D64 | **The PII gate blocked the capture commit on a vendored skill's schema.org example; narrowed rather than suppressed, and proved in both directions** | `.agents/skills/seo/SKILL.md:263` ships a JSON-LD `ContactPoint` example whose `telephone` value sits in the range NANP reserves for fiction, beside `https://example.com` and `linkedin.com/company/example` — a documentation placeholder arriving with `addyosmani/web-quality-skills`. **The gate was right to fire and the rule was not touched.** `ALLOW` gained one entry requiring **both** the `"telephone":` JSON-LD key **and** that reserved prefix. Proved in both directions per the loosening rule, not assumed: (1) the vendored example now passes, 103 files clean; (2) `--self-test` still reports *"the gate bites and does not cry wolf"*; (3) a planted **real-looking** number inside a `telephone` field is still caught; (4) a planted **reserved-range** number outside such a field is still caught. **Two unplanned proofs that it bites, both self-inflicted:** it went red on `tests/no-pii.mjs` itself when the justifying comment first spelled a realistic number out, and red again on **this very ledger row** when it quoted the two probe values. Both were reworded to describe the shape rather than print it. A check that catches its own documentation, twice, is a check reading the bytes rather than a rule nodding along | 08-11 |
| D63 | **Panel images captured: SaafSaans taken LIVE rather than from its stored screenshot, in English and Hindi; NarraTwin's real capture measured unusable and replaced by a labelled interface design** | **The stored SaafSaans screenshot carried `SAMPLE — not a reading`** and the owner asked for that removed. **I disagreed with cropping it and said so**: removing Quorum's `example.com` sources leaves what remains independently true, but removing SaafSaans' SAMPLE badge changes the meaning of the AQI and risk numbers still in frame — the difference between cropping an empty car park out of a shop photo and cropping the CLOSED sign off the door. **Resolved by measurement instead of by argument:** all three apps answered (`saafsaans` 200/0.46s, `quorum` 200/1.1s, `citevyn` 200/7.9s cold), so SaafSaans was captured live. It now reads `● LIVE · 5:00 PM · AQI 270 · POOR` with **no sample label at all** — and `EXAMPLE PERSONA` turns out not to be a disclaimer but an invitation (*"Fill in your own details and the advice becomes yours"*). Nothing needed hiding. **Hindi captured too, at the owner's request** (`/?theme=light&lang=hi`) — it carries a bilingual warning that no Hindi speaker has reviewed the translation, with a specific carve-out for medicine and inhaler information, which is a stronger product-safety artefact than anything the previous panels held. **Both hero crops stop above the time-window strip**, which shows a known defect the owner is fixing (at 5:00 PM it recommends 9 AM–12 PM); nothing left in frame depends on it, and the full hero is re-captured when the fix lands. **NarraTwin: the plan's first step was to extract the real capture embedded in `reports/lighthouse/stage8-lighthouse.json` and judge it on sight — done, and it is unusable**: 412×4224 of unstyled HTML with bare form controls, no CSS applied. Fell back to the owner-supplied interface design per the recorded fallback, **captioned as a design and not a running capture**, face blurred. **First blur pass was wrong and re-cut:** it blurred the whole tile including `FICTIONAL SYNTHETIC STILL · NO REAL-PERSON REFERENCE`, destroying a disclosure to hide a face; the second pass blurs only the face and leaves both labels crisp. Quorum uses its stored acceptance run cropped above the `example.com` fixture sources, keeping `41.2s`, `actual $0.188 (approved $0.19)` and `4 of 4 aligned · 3 revised their position`. Five WebP files, **240 KB total**, in `src/assets/projects/`; `no-pii.mjs` green over 103 files. **Also recorded: the two supplied NarraTwin files are byte-identical** — `-viewport` and `-full` are the same 222,265-byte image, so no full-page version exists | 08-11 |
| D62 | **StackClimb is defined as "where Rohit Agrawal builds independent AI systems — outside any employer". "Product studio" is dropped** | Replaces D60's line at the owner's decision, after I argued it as a **hiring risk rather than a style preference**: "studio" reads as a business, and a hiring manager screening for a full-time senior/principal hire silently asks *is he available, or running something and hedging?* — a question that costs interviews and is never voiced. It is also unverifiable: there is no studio, there is one engineer shipping systems, and the site's own rule is to state what is proven. The replacement defines the name, states independence, carries no unverifiable claim, and removes the availability ambiguity. **The owner's second and third sentences are kept verbatim** — *"Independent projects are built outside any employer. Employer outcomes are attributed to their employer and marked approximate."* — they do real work and state the two-ledger discipline in the footer. **Also decided:** artefact panel on the home band, **screenshot on the project page**, not both in the same place. Reason: six app screenshots is what every portfolio has, and a recorded run with a file path and commit SHA is what almost none have — the screenshot is the commodity, the artefact is the moat. Permitting screenshots on project pages needed `DESIGN.md`'s "no photographic texture" ban relaxed for that surface. **CLOSED the same day by D66** — `DESIGN.md:345` carries the carve-out. This clause read "open, not yet amended" for two days after it had been amended | 08-11 |
| D58 | **World A — the painted plate world — chosen over a light editorial world, by rendered comparison, not description** | Both were built as the same section ("Two ledgers, deliberately kept apart") in a throwaway `mockup/`, screenshotted at 1440×900 and 390px, and looked at. **The owner's reasoning, recorded as his:** *"A is aligned with the current site layout… first, let us build it on what is already present."* Mine, agreeing: the diagnosed problem is **structural, not stylistic** — a catalogue with no argument — and World A delivers the whole content model without touching the look, so taking B would change structure and styling together and make a bad outcome un-attributable. **One premise corrected before the choice was taken:** B is *not* a light/dark-mode revamp. The site is dark-only today; B would be light-only. Either way it is one fixed look, so **choosing B would not have delivered light/dark mode** — that is D30, about honouring `prefers-color-scheme`, and neither world addresses it. B's real cost is a new `DESIGN.md`, a new `palette.css`, ~14 contrast pairs re-derived, four colour gates re-tuned and 56 baselines. **B is deferred, not rejected** — if the structure ships and the site still reads wrong, B becomes an informed decision rather than a guess. **Not blended:** per `impeccable`, the losing world is deleted, never grafted on. One carry-forward from the design hook: both worlds used a 2px left border on the systems list — the `side-tab` AI-tell — and the real build uses a hairline rule or indent instead | 08-11 |
| D57 | **The home page becomes an argument, not a catalogue — six acts — and the nav, project depth and card model change with it. Overrules D28 and D32** | Owner's finding: *"I'm not getting a way where we are giving a holistic view to the recruiters in the 60 or 90 seconds of scanning… something at core is missing."* Correct, and it is structural: seven plates arranged as a contact sheet is a catalogue, and a scanner wants to be walked somewhere. **Acts:** 00 hero · 01 professional proof (two ledgers) · 02 what I built · 03 engineering model → `/how-i-build` · 04 career evolution → `/experience` · 05 contact. Governing rule: *home is the argument, subpages are the evidence.* **Nav → `PROJECTS · EXPERIENCE · HOW I BUILD · CONTACT`**, amending D28: the owner rejected D28's `WORK` because a recruiter reads it as employment history when the plates are independent projects; `SYSTEMS` was then rejected as precise but not guiding. Hero CTAs → `What I built · Career evolution · Email me` ("evolution" not "evidence" — *evidence* is the word the two-ledger act owns). **`/experience` is a real page, overruling D32** — a bird's-eye career evolution with the complete CV as a download from inside it; D32 predates `/how-i-build` and assumed the career narrative had nowhere else to live. **Project depth: two levels** — each project becomes `/projects/<name>` built from its existing plate, giving a URL a recruiter can be sent to directly; no middle ecosystem page, which with six projects is a redundant hop. **Card model: signature proof first** — one memorable verifiable fact leads, then what it is, then Works now / In development / Not claimed, then links. The material already existed in the wrong order: `index.astro:150-213` carries CiteVyn's `52/52` artefact and the refusal case, but the paragraph read first. **Grouping: two groups, not the supplied mockup's "four layers"** — *Use them now* / *Being built*, under "What you can use, and what is still being built". The principle behind "layers" was right (identical cards imply equal reality) but "layers" is jargon the voice rule bans, four groups over six projects gives groups of one, and "Foundations"/"Supporting assets" are labels nobody scans toward. **Published AI skills are a band on `/how-i-build`, not a nav slot and not in the project list** — two public repos today (`project-doc-skills`, `.github`); **threshold recorded: at three, it graduates to its own home act**, so it is a decision rather than drift | 08-11 |
| D56 | **Review findings against D55 closed: motion test coverage, three stale plan-status docs, and one overclaim in D55's own text, corrected** | A review of `hero-animations-wordmark` found D55's motion/toggle work shipped with zero automated coverage despite the plan's own Verification item 3 requiring Playwright tests written RED-first — closed with `tests/motion.spec.js` (12 tests, both viewports): toggle default-ON and `aria-pressed`, keyboard `Enter` activation, `localStorage` persistence across reload (both directions), `prefers-reduced-motion` overriding the toggle even when left ON, and the DEF-1 fallback (below-the-fold plate content stays `opacity:1` with JavaScript disabled). Every case proven to bite by mutation — each assertion was watched to fail against the un-fixed source (localStorage write removed, `aria-pressed` sync removed, the reduced-motion CSS block *and* its JS guard both removed together, the DEF-1 fallback rule removed) before being restored green. One assertion changed shape mid-build: `test.use({ reducedMotion: 'reduce' })` measured NOT to apply the media query in this repo's Playwright 1.62.1 config (`matchMedia(...).matches` read `false` on repeat runs); `browser.newContext({ reducedMotion: 'reduce' })` measured to work every time and is what the test uses instead. Also closed: `plans/001–003-*.md` and `plans/README.md` still said `Status: TODO` for three items D55 already shipped — corrected to `SHIPPED (D55, 08-11)`. And a correction to D55's own text: it repeats the plan's framing that the hero pull-quote/proof-strip "fills the whitespace gap" — measured live at 1440px, it does not: `.plate-figure` bottom sits at y=565.6, `.plate-copy` bottom at y=636.6, a **71px gap remains**, the same order of magnitude as the ~90–100px defect the plan set out to close. Not a regression (the old Artefact card was taller and caused it too) and not blocking, but D55 overstated it — recorded here rather than silently rewriting D55. Separately, `tests/tap-targets.spec.js`'s `/cv` mobile check was flagged as flaky at the 44px boundary during review; re-run 9 times (single-file and full-suite) here, on this machine, and it stayed green throughout — noted as unreproduced on this run, not as fixed, since nothing in this PR touches tap-target sizing | 08-11 |
| D55 | **The full `resilient-hatching-peacock.md` plan shipped in one PR: voice fixed to first person, the hero rewritten with a bookend lede and a pull-quote, the refusal card relocated to CiteVyn (O-HERO resolved above), the `Plate Nº` label removed sitewide, Phase 2 scroll/hover animations plus a persisted footer toggle, the real StackClimb wordmark, and four dead figure components deleted** | Every item verified by running the thing, not reading about it (this plan's own rule). `grep -c "Plate Nº" dist/` → 0; the demo/release line appears exactly once in `dist/index.html`; the hero section contains zero "Refused"/`claude_api_006` text and the CiteVyn plate renders two `Artefact` cards with the original data intact. `npm run build` clean. Full gate suite: `no-pii` (self-test + real, both green), `file-budget` (self-test + real, both green — required trimming comments in `index.astro` and `Layout.astro` to stay under their recorded ceilings), 104 Playwright specs passed / 2 skipped by design (`tests/visual-baselines.spec.js` is the one exception — see below), `boundary-check.mjs` worst seam 7.56:1, `nav-contrast.mjs` worst 6.26:1 at both viewports. Motion verified live: `document.getAnimations()` returns entries mid-reveal and the `.in-view` class lands on scroll; the footer toggle defaults to `aria-pressed="false"`/"on", a keyboard `Enter` flips it to `off`, persists across a reload via `localStorage`, and both `prefers-reduced-motion: reduce` and JavaScript-disabled contexts show every plate fully assembled immediately (computed `opacity: 1`, no transform) — never hidden-by-default. Lighthouse mobile, 3-run median: **Performance 100, Accessibility 100**, CLS 0.009 (hold-the-line gate honoured, not relaxed). **One real defect found and fixed during verification, not before:** the wordmark image plus "Rohit Agrawal" on one row overflowed the nav bar at 390px (menu button landed 73.6px past the viewport, measured) — fixed by wrapping the name onto its own row at ≤900px rather than hiding it, because hiding it would have made `.brand` an image-only link and tripped `nav-contrast.mjs`'s "visible but no glyph rendered" check, the exact DEF-38/DEF-46 failure shape. **`tests/visual-baselines.spec.js` fails locally on every viewport against the committed `-linux.png` baselines** — expected and by the file's own header comment ("Baselines are NOT hand-generated on a laptop"): the layout changed on purpose (wordmark, hero, plate-tag removal, second Artefact card), so the Linux baselines this PR needs regenerating via `workflow_dispatch` with `update_visual_baselines: true` — never by hand on macOS, per the file's own warning. That run happens as part of landing this PR; if this note still reads "needs regenerating" when you read it, it has not happened yet. Wordmark asset: cropped from `assets/inbox/brand/wordmark-dark.png` (untracked staging, never committed from there), background stripped to transparency by distance-thresholding against the sampled corner colour (82.2% of pixels cleared, corner alpha 0), downscaled with a box (area-average) filter — bounded by construction, cannot overshoot, same reasoning D48 recorded after Lanczos ringing on the icon. No recolouring: white measured 255/255/255 before and after; orange within 10/255 of source, an ordinary resampling blend at the stroke edge. Ships as `src/assets/wordmark-nav.png`, content-hashed via `src/assets` per D48's pattern | 08-11 |
| D54 | **The Codex companion review of `pr5-astro-major-bump` was killed (exit 144) mid-run, before it reached Playwright, boundary-check or nav-contrast — the full gate suite was re-run locally to close the gap it left, not to re-litigate DEF-20** | Codex got through `npm ci`/`npm run build` (both failed inside its sandboxed review environment on filesystem-write restrictions — an environment artefact, not a real failure) plus `no-pii.mjs` and `file-budget.mjs` (both green), then was terminated while installing Playwright's browser. Re-run here, unsandboxed: `npm ci` clean, `npm run build` green (3 pages), `no-pii.mjs`/`file-budget.mjs` self-tests and real runs all green, `npx playwright test` **78 passed, 2 skipped** (the coarse-pointer skips are by design), `boundary-check.mjs` worst seam **7.56:1**, `nav-contrast.mjs` worst **6.26:1**. Every number matches what DEF-20's row below already recorded on 08-10 — this run is independent corroboration, not a new fix; no code changed | 08-10 |
| D53 | **The repo went public on `gh repo edit --visibility public`, owner's explicit instruction** | Gated on DEF-50 being closed first: history rewritten (`git filter-repo`) to strip the visiting-card blobs carrying a personal phone number and LinkedIn QR, repo recreated fresh (old one archived privately as `stackclimb-website-archived-20260809`) so no GitHub PR ref could still reach the blob. Verified clean before the flip: fresh `git clone --bare` of the new repo showed only `refs/heads/main`, no `docs/brand/candidates` in any object, `gitleaks detect` clean on 85 commits, no repo secrets/deploy keys/webhooks configured. Confirmed public after: `gh repo view` reports `visibility: PUBLIC` | 08-09 |
| D52 | **The polish pass deleted the leader-line machinery — measured dead, not judged dead** | `/impeccable polish`, 08-09. `data-target` and `data-anchor` appear on ZERO elements in the built site; the only grep hits were the script's own template literals. The wired-caption interaction D49 assumed "remains valid for caption strips" had no consumers anywhere — plates.js attached every listener to nothing, and ~80 lines of JS plus the `.leads`/`.lit` CSS shipped as weight claiming an interaction that cannot occur. Deleted; D49's sentence corrected in DESIGN.md; implementation preserved in git history. Everything else the pass inspected — focus rings, menu states, all eight plates both viewports, the contact closer — held the bar; DEF-33 re-verdicted stale on the same evidence | 08-09 |
| D49 | **The artefact panel is the site's signature; the drawn figure is superseded as the north star** | The full-page critique (dual-agent, 4 assessments, snapshot `2026-08-09T10-02-30Z`) found DESIGN.md canonizing figures with leader lines while six of seven plates ship none — the lit panel quoting real recorded output replaced the figure, and the critique judged the replacement stronger: "a quoted refusal beats an illustrated one." DESIGN.md now records the supersession above the original text rather than deleting it. Figures stay valid where they exist; the leader-line machinery stays for caption strips | 08-09 |
| D50 | **Grounds may serve as semantic ink on lit surfaces — the One Thread Rule is amended, not broken** | The GO/NO-GO chips had already borrowed plate grounds as verdict colours (bordeaux stop, viridian go) against the written ochre-or-nothing rule. The critique's verdict: elegant, palette-internal, and colour-blind-safe because every chip carries its word. Codified in DESIGN.md with its scope: lit surfaces only; on grounds and chrome the One Thread Rule stands | 08-09 |
| D51 | **All six page-level critique findings fixed the same day, one PR each (#20–#23), every fix measured** | CV measure 150ch → 78ch (the 52rem trick loses the cascade to `.plate`'s padding shorthand — cap moved to content). Hero 1,107 → 900px with the ledger above the fold; project names demoted to the documented `.tight` step (the hero's 59px was the RECORDED deliberate choice — 96px ran to 8 lines — so the inversion fix went down, not up). Overview on its own umber ground, NarraTwin 8% → 22% saturation — after which **the axe gate caught 8 AA failures on the new surface** (parent-opacity-dimmed ochre at 4.16:1, 52% neutral state at 4.19:1), fixed by moving dimming to color-mix and raising the step to 62%. Mobile dead zones 94 → 40px — most of it was desktop tag-clearance padding applying below 900px where the tag is static. Type floors: provenance 9.9 → 11.2px, captions 9.6 → 10.56px | 08-09 |
| D48 | **The owner's chevron mark ships in the nav, as supplied, with only the baked background stripped** | Owner's decision 2026-08-09, taken **over** the comparison's recommendation to adopt nothing — his call to make, and it stands. The conflict was raised before compliance, per the rule: the comparison's two measured problems were shown (baked `#1a2338` background rendering as a lighter rectangle on the `#0e1322` plate; accent `#f05020` as a second hue against the derived ochre). He approved one amendment: the background stripped to true transparency programmatically — 71.7% of pixels cleared, corner alphas 0, shape and colours otherwise bit-identical to his file. **Scope**: the nav lockup only (logo beside the Bodoni name — the one combination the comparison found workable), which also gives the phone its first visible mark. The favicon stays the RA plate: at 16px the chevrons collapse, and that is a measurement, not a preference. The orange ships as supplied and is now a recorded second accent by his decision. **Amended the same day, owner-approved, after `/impeccable critique` ran dual-agent (4 isolated assessments) pre-merge:** the mark's neutral strokes are warmed from pure white to bone `#f2ebdd` — Assessment A measured two white points in a bone bar as the one thing making the mark read placed rather than native. Recoloured at native resolution then downscaled with a non-overshooting kernel (Lanczos ringing pushed edge pixels 13/255 past bone on the first attempt); brightest stroke core now within 3/255 of bone. Also from the critique: a 1px optical nudge (measured 0.9px low) and true intrinsic width/height attributes. Orange untouched — measured at 12.4% of the mark's pixels, below the threshold of harm. Snapshot: `.impeccable/critique/2026-08-09T10-02-30Z__src-layouts-layout-astro.md`. Asset content-hashed via src/assets (not /public — the DEF-24 trap), 6,574 bytes | 08-09 |
| D47 | **The new gates went through the full two-round adversarial review, and round two found a live CI blocker in a gate I had called done** | Round 1: Codex (`gpt-5.6-sol`), short bounded prompt, five findings — three real (single-href collapse, address checked only in `#contact`, `preventDefault()` unprobed), one already-defended (bar links at `opacity:0` fail the pixel gate as "no glyph rendered"), one design-tension resolved by re-imposing full visibility outside the header. Round 2: same-context agent, executing every mutation — **the branch itself failed `nav-contrast.mjs` 92 positions**, because a closed `<details>`' absolutely-positioned links keep their layout box in Chromium; I had run the seam gate and not the nav gate. Also real: `isVisible()` reads boxes not paint (opacity-0 menu passed), a derived nav population shrinks with the mutilation it should catch (partner literals `/cv` and `/#contact` now pin it), `git ls-files -- src tests` let untracked and top-level files escape the budget, and `overflow: hidden` froze the page with every gate green (a scroll probe now exists). All closed and re-proven by mutation. **Cap honoured: two rounds, stop.** Residual, written down not hidden: focus-ring CONTRAST (vs pixel-change) is asserted only on the nav; the tap gate's inline-display carve-out is dodgeable by styling a control `display: inline` | 08-09 |
| D46 | **The mobile menu is a native `<details>` disclosure, not a button-plus-script** | DEF-42's fix. Three shapes considered and two rejected in RCA-004: a permanent second row of links eats 11% of an 844px viewport on a page of 100svh plates; a JS panel is a control that dies with the script, which is DEF-1's exact failure on a site promising near-zero JS. The `<details>` opens, navigates and closes-by-reload with JavaScript disabled; `src/scripts/nav.js` adds close-on-navigate, Escape and click-outside as enhancement only. Both menu surfaces render ONE array in `Layout.astro` — a second hand-typed list would be the fourth DEF-10-shaped narrowing. The email chip moves into the panel below 900px: measured, the bar at 390px has 65.8px free and the chip is 111px wide. Held by `tests/nav-reach.spec.js`, which activates every destination at every viewport — three mutations red, watch-it-fail in the PR | 08-09 |
| D40 | **Phase 0 ran. The headline survives; the lede does not** | `docs/positioning-phase0.md`. D33 said every 08–09 Aug copy recommendation was unaided judgement and that Phase 0 existed to test them. It did. **D23's headline is kept** — tested against `brand-positioning`'s own red flags it partially fails "differentiator is a table stake" (every RAG product claims citations now) but passes "substantiated by proof points" strongly, and *"refuse to fake it"* is the half nobody else claims. Replacing an owner-authored line on a partial fail would be overreach. **What changed is the lede.** The site described fourteen years as *"…across distributed systems and production reliability, **now** building independent AI systems"*, which reads cold as *career changer, eight months in* — the weakest reading of his strongest fact. The tested position: AI release governance is an unclaimed quadrant, and four of his six systems **are** gates. Also settled: `Seeking` five titles → one, and the contact plate opens with the owner's own demo-vs-release sentence | 08-09 |
| D41 | **The CV's employment data is now in hand. D31's blocker is cleared** | `Rohit_V3` carries title, dates and scope for all six employers: Oracle Principal MTS Apr 2019–Apr 2026 · Amazon QA Engineer II May 2017–Sep 2018 · LimeRoad Senior SDET Oct 2015–May 2017 · Mobileum Senior STE Apr 2015–Sep 2015 · Snapdeal QA Engineer II Jun 2014–Apr 2015 · Subex Product Specialist Jul 2011–Jun 2014. Plus education (BE Information Science, VTU, 2011), three certifications, and five awards. **Nothing about the CV needs inventing any more.** Every percentage in it is self-reported and ships labelled | 08-09 |
| D42 | **The H-1B ships as WORK AUTHORISATION, never as intent — and always after relocation worldwide** | Owner's instruction, 09 Aug: *"I have approved H-1B, but this should not be portrayed like I am only looking for USA opportunities. It should convey that I have it but I am open to relocation worldwide."* The previous session flagged it and correctly refused to publish it unasked, since visa status is personal information. **The fix is framing, not wording.** "H-1B approved" standing alone reads as a destination; a row labelled `Work authorisation` listing **India · United States** reads as paperwork already settled, and relocation-worldwide keeps the headline position above it. A US recruiter gets the answer to "must I sponsor and wait" without any other reader being told the search is American. It also sits exactly inside the amended voice rule (D24): not his urgency, not a pitch — the removal of an objection the **reader** has. Placed on the contact plate and `/cv`, deliberately **not** in the hero, so the most-scanned surface never leads with one country | 08-09 |
| D43 | **`/impeccable critique` ran dual-agent and scored 23/32, up from 21/32** | Run 09 Aug, two isolated sub-agents per the command's own hard invariant and per D7 — the builder is never the auditor. **Gains are real and large**: progressive disclosure now passes (the overview closed *"nothing in the first viewport says what the systems do"*), the grey placeholder bars are gone, and micro-type dropped from 82.6% of characters under 14px to 35.2%. **Held down by three items the previous critique already named and which are unchanged** — no active-plate marker, the mobile nav, micro-type — plus two defects the new surfaces introduced (DEF-45, and the no-op tone classes). The measured Lighthouse baseline it established replaces an assumption: **home mobile Performance 100, Accessibility 100**, not the 98 `ACTION-PLAN.md` assumed. Ten points of headroom for Phase 2, and the gate should read "no worse than 100" | 08-09 |
| D44 | **The cross-model review finally ran, and found five real holes in gates I had called strong** | Codex (`gpt-5.6-sol`) via `codex exec --sandbox read-only`. Two earlier attempts hung for hours and were killed; the cause was my prompt — long, ten enumerated categories, at reasoning effort `high`. A short, bounded prompt asking for a ranked top-5 returned in minutes. **All five verified against source before acting, and all five were real**: (1) `.site-nav a { opacity: 0.2 }` passed because the gate read computed `color`, which opacity does not affect — the DEF-38 defect passing the gate written to catch it; (2) `links.spec.js` did not recurse, so a nested `/projects/foo` was never tested — the same silent narrowing as DEF-10 and DEF-44; (3) `href="#"` skipped both checks, because `''` is falsy; (4) resting-state-only contrast, now DEF-46; (5) `preventDefault()` on the skip link passed, because the test proved the link existed and was focusable but never **activated** it — DEF-28's lesson one level deeper. Four of the five are closed and mutation-proved. **This is the measured case for the rule**: same-context subagents reviewed these files and found none of it | 08-09 |
| D45 | **The third render is what makes rendered-contrast measurement honest** | Building DEF-46's fix, the two-render version (as-is, glyphs removed) looked complete and was wrong in the one case the gate exists for: **when text is nearly the colour of its backdrop, the two renders barely differ, so low contrast is indistinguishable from NO TEXT.** Measured — a deliberately awful `#c49a45` on `#c99b3f` hover reported "no text found" and scored **green**. A third render forces the glyphs to a known probe colour, which locates them independently of their real colour and recovers per-pixel coverage `a = (M − B) / (probe − B)`. Recorded because the two-render version is the obvious design and it fails silently in the direction of passing | 08-09 |
| D1 | **Extend the portrait-plate world, do not replace it** | Execution is strong; the problem is structural — "one plate, one viewport" cannot hold a blog post or a long résumé. Split by job instead | 08-07 |
| D2 | **Cloudflare Pages for the apex; Fly.io for app subdomains** | Static requests free and unlimited; same vendor as DNS; apex CNAME flattening. Fly idle cost ≈ $0.08/app/month | 08-07 |
| D3 | **Spend nothing on warm machines until a cold start actually hurts** | Owner's call, and correct — no traffic exists yet | 08-07 |
| D4 | **Keep Astro** | Content collections fit blogs and docs; islands give interactivity without a framework runtime | 08-07 |
| D5 | **Availability language stays; playbook language goes** | Saying "roles sought, open to relocation" is ordinary. Publishing the success metric is a playbook | 08-07 |
| D6 | **Private work is framed by a standard, not an adjective** | "Big" or "ambitious" would be an unearned superlative. "Judged on finished work rather than intent" says the same thing and is true | 08-07 |
| D7 | **The builder is never the auditor** | `impeccable` builds and audits. Its audit may "only recommend commands from: /impeccable …", so a problem outside its command set is one it cannot name | 08-07 |
| D8 | **File budget: 250 lines / 32,000 bytes / 120 chars** | Inherited from NarraTwin `ADR/0047:55`. Pre-existing files grandfathered at 500 and may not grow | 08-07 |
| D9 | **Review is capped at two rounds, with a circuit breaker** | A new class of finding every round means the miss is upstream — requirements, planning, or understanding. Patching cannot fix those | 08-07 |
| D10 | **Never write a skill that exists in public** | Public skills from known authors are tested by many people. One written here in an afternoon is shallow | 08-07 |
| D11 | **Plain English, lead with the answer, no AI filler** | Owner had to ask twice. Now a rule loaded every session | 08-07 |
| D13 | **Launch gate: SaafSaans copy + clickable contact links only.** Contrast, the 12 unchecked numbers, CI, hooks and the repo history all land *after* launch | Owner's call: live and honest, fast. Neither deferred item is *false* — one is a quality gap, the other unconfirmed-but-probably-right | 08-07 |
| D14 | **CORRECTED 08-09: the project is NOT connected to the repo.** ~~Deploy via Cloudflare Pages connected to the private repo (approach A)~~ | Only option that ships today. B risks deploying a stale `dist/` (DEF-11 is exactly that failure). C puts a red, partly-broken suite on the critical path | 08-07 |
| D15 | **Approach C is the target. A is a recorded debt with a written exit condition** | See the standing notice at the top of `AGENTS.md`. A temporary state nobody mentions becomes permanent | 08-07 |
| D12 | **Skills to install** (below) | Three agent reviews, applying the how/where-not/what-not criterion | 08-07 |
| D16 | **The SaafSaans origin ships with no duration at all, rather than a labelled one** | The approved spec (`specs/2026-08-07-launch-scope-design.md:36`) allowed the three-hour figure if labelled owner-stated. Dropping it is better: the site's whole subject is claims that survive a check, and a labelled figure still invites the reader to check a number that cannot be checked. What replaced it — entered at the event, as an app that already existed, rebuilt over the next three days — is checkable line by line in `saaf-saans`' commit log and case study | 08-08 |
| D39 | **The PII rule is enforced by a gate, not by prose** | I wrote D38 and put its enforcement in a markdown file. `AGENTS.md` says in its own words that enforcement is mechanical — CI and hooks — never prose. The owner caught it. `tests/no-pii.mjs` now blocks Indian mobile, international, Aadhaar and PAN shapes across `src`, `public`, `docs`, `dist`, `tests`, `assets`. **Proven to bite:** planting the real number in `Layout.astro` makes `git commit` exit 1 and leaves HEAD unchanged. `--self-test` proves both directions — 2/2 planted numbers caught, 0 false positives on `IST (UTC+5:30)`, commit shas and counts. Its own fixtures are assembled from digits at runtime, so **no file in the repo contains a phone-shaped string** and the scanner needs no self-exclusion — the first two attempts failed on themselves, which is how that was found | 08-09 |
| D38 | **The phone number never goes on the public `/cv` page. Email and LinkedIn only** | Verified 09 Aug: the number is **not currently public** — not in `imrohitagrawal/README.md`, not tracked in this repo, not in the built site, which exposes exactly three routes (email, LinkedIn, GitHub). Publishing it would be a **new and irreversible** exposure: an indexed 10-digit number is scraped within days, and Indian mobiles are heavily targeted. It also buys almost nothing — nobody cold-calls a principal engineer off a website; a recruiter emails first and the number comes out in the reply, when he chooses. **Three "clever" fixes are banned as theatre:** reveal-on-click JS (scrapers execute JS), the number as an image (OCR reads it, screen readers cannot), and `@media print` (the number stays in the HTML source, so every scraper gets it and every human does not). Omitting it is the normal shape of a published CV and touches no honesty rule — he is choosing a channel, not misstating a fact | 08-09 |
| D36 | **`Rohit_V3` is the authoritative CV. It wins every conflict with `FINAL-Rohit_Master_Resume`** | Owner's instruction, 09 Aug, recorded verbatim in `assets/inbox/README.md`. The `.md` file is the convenient one to parse and convenience is not authority; it fills gaps only. Conflicts get both values recorded here rather than silently resolved | 08-09 |
| D37 | **The entire `assets/inbox/` is untracked, not just `brand/`** | Widened the same day it was created. A scan of the uploaded CV found a **10-digit phone number** in `FINAL-Rohit_Master_Resume.md`, and under the original rule `photo/` and `resume/` were tracked — the next `git add -A` would have put it in a repo being prepared to go public. Nothing reached a commit; the files were still untracked. **Third occurrence of this class:** visiting cards on 08-07, a CV now. The rule is now the simplest one that cannot fail | 08-09 |
| D35 | **`Paramchoudhary/ResumeSkills` (1,477★, MIT) is the CV toolkit; `wonjyou` stays uninstalled** | Second search round, 09 Aug, after the owner asked whether anything better existed. It does: 1,477 stars against wonjyou's 5, and a real licence against none. Take five of its 22 skills. **Skip `career-changer-translator`** — its worked examples are Teacher→Corporate and Military→Corporate, and the owner's move is a specialisation shift *inside* tech, so it would over-apply. `MadeByTokens/resume-helper` is philosophically the closest match to this project (adversarial multi-agent: Writer, Fact-Checker, Interviewer, Coach) but has 6 stars and is not worth the risk | 08-09 |
| D34 | **Positioning skills: arnabbagxd (4 skills) + borghei brand-strategist. wonjyou read, not installed** | Searched 09 Aug. `coreyhaines31/marketingskills` rejected despite 43,542★ and a known author — **not one of its 50 skills covers positioning**; stars are not relevance. `wonjyou/portfolio-review-skill` is the closest match to the 30-second test and was rejected on **5★ and no licence at all**, which by default means all rights reserved and cannot be vendored into a repo going public. **Collision verified:** zero clashes with the 38 installed skills; `brand-guidelines` exists in both new repos and must be taken from neither | 08-09 |
| D31 | **The CV is a `/cv` page that prints cleanly, not a PDF upload** | Owner chose approach B. A PDF goes stale silently and nobody notices; a page is indexable, always current, and a recruiter can still print it to PDF and forward it. **No longer blocked — see D41.** `Rohit_V3` carries title, dates and scope for all six employers, so nothing needs inventing | 08-09 |
| D32 | **No separate experience page. The experience band goes on the Approach page** | A job-history list says where he worked; the Approach page says how he builds and shows the fourteen years that produced the method. Four destinations dilutes a 30-second read. **Real gap it exposed:** "led and mentored 11+ engineers" appears **0 times** on the live site, and for senior/principal roles scope of responsibility is often the first filter | 08-09 |
| D33 | **Every recommendation made on 08–09 Aug is unaided judgement, not skill-validated** | Recorded so it is not mistaken for tested. The headline, the nav rework, the photo placement, the no-experience-page call and the CV shape were all decided before any positioning skill was installed. Phase 0 exists to test them and some may not survive it | 08-09 |
| D27 | **The photograph goes in the hero, avatar-sized, beside his name in the lede** | Owner's decision 09 Aug, and it reverses my recommendation of 08 Aug. He originally instructed this on 07 Aug — "I am giving you a photo of mine, you can replace that as a human being" — and I wrote it up a day later as an open question rather than as the reversal it was, which a completeness audit caught. Measured constraint that decided the placement: the hero plate is 1091px in a 900px viewport, already **191px over** `DESIGN.md:199`. Beside the name is the only spot that fits without dropping the proof card. Owner: "even if it's not a portrait, it is fine, it can read as an avatar" | 08-09 |
| D28 | **Nav becomes `WORK · APPROACH · CONTACT · Email me`. "Private" leaves the menu** | **The owner found this; no tooling did.** A dual-agent critique, two review agents and a cross-model pass all read this page. The critique scored "match between system and the real world" at 3/4 and flagged the garment captions while walking past a public menu item reading `PRIVATE`, which invites a visitor to wonder what confidential thing is on show. It is also the H7 fix: the nav reached 3 of 7 plates. Private work becomes a row on the overview with `closed` as its state — same information, no alarm | 08-09 |
| D29 | **"Do not optimise animation" does not mean "do not add animation"** | `docs/STATUS.md` carries a bolded instruction not to optimise scroll, animation or the blend overlay, written when the site measured Lighthouse desktop 100 / mobile 98. I wrote a Phase 2 that contradicted it and set a gate of 94 — four points below the current measurement — without noticing. Adversarial review caught both. Owner confirmed the rule guards against micro-tuning a fast site, not against purposeful motion. **Gate is now "no worse than 98"** | 08-09 |
| D30 | **Light and dark theme: deferred, not rejected** | Owner: finish the agreed work first. Cost is a second colour system — seven grounds and seven surfaces re-derived, the value ladder rebuilt, ~14 contrast pairs re-checked, and `data-theme` is already taken by plate theming. It moves the 30-second test not at all. **Counter-argument recorded rather than buried:** the site honours `prefers-reduced-motion` and ignores `prefers-color-scheme`; respecting one stated user preference while ignoring the other is the weakest part of this position | 08-09 |
| D23 | **Headline becomes the full positioning line: "AI systems that show their work and refuse to fake it"** | It is already his own line in `PRODUCT.md`. The current headline truncates it and claims half. "From customer ambiguity to production evidence" is kept as a candidate for the Operating Model page, not the home hero — it describes his process, not his systems | 08-08 |
| D24 | **The voice rule is amended: his urgency is banned, the reader's problem is not** | I read "never pitches, never signals urgency" as forbidding copy that names a reader's pain, and raised it as a blocker. The owner drew the distinction I had collapsed and he is right. Recorded in `AGENTS.md` with a table of allowed and not-allowed, so the next session does not re-litigate it | 08-08 |
| D25 | **Build an Engineering Operating Model page, with labelled employment evidence** | The strongest answer to "is the AI move real or four weekend projects" — fourteen years of practice, not four repos. Already implied by `PRODUCT.md:85` and by D1. Oracle's MTTD −35% and cycle-time −25% ship **labelled self-reported**; the owner's call is that employment evidence is what people trust | 08-08 |
| D26 | **"Shift-left by default, shift-right by design" leaves the hero and moves to the Operating Model page** | True to him and in `PRODUCT.md`, but it is QA vocabulary and the single phrase most likely to mis-bucket him as a test lead in a thirty-second skim. The substance stays in the hero in plain words; the phrase appears where context defends it | 08-08 |
| D21 | **Every figure carries a real, dated artefact — one shared `Artefact.astro`, not five drawings** | The four project figures drew grey lorem bars, which against the new near-black grounds read as an unfinished wireframe on a page headlined "AI that shows its work". Each now quotes a named file at a named commit: CiteVyn's recorded golden run, Quorum's readiness decision, SaafSaans counted at HEAD, NarraTwin's No-Go. Rule recorded in `DESIGN.md` as The Lit-Surface Rule | 08-08 |
| D22 | **Quorum-AI's hand-authored fixtures were rejected as a source** | `_handoff-s4-golden-draft/golden-cases.json` carries a full four-model synthesis and would have made a convincing figure. Its own metadata reads "HAND-AUTHORED, REAL-SHAPED fixtures... NOT captured production runs. No case is a real run." Using it would have put invented system output on the site. The readiness review was used instead | 08-08 |
| D18 | **The value ladder replaces the flat palette. Portrait Plates is kept, not discarded** | Owner's call after `/impeccable critique` scored 21/32. Six of seven grounds sat in a 14-point lightness band, so the page read as printed rather than lit — that, not hue, is what "dark and dull" and "1980s, 1990s" described. Hues are unchanged at 41–47% saturation; only lightness moved. Recorded in `DESIGN.md` under The Value Ladder | 08-08 |
| D19 | **The hero mannequin is gone, replaced by real CiteVyn output** | Owner: "childish", and "I always forget this dummy". It also broke `DESIGN.md:258` — "Don't draw people" — which the design system had already written down and not enforced. The replacement is a verbatim refusal from `citevyn/backend/artifacts/golden_report.json`, case `claude_api_006`, recorded 17 July 2026. Not written for the website: a page promising "AI that shows its work" cannot illustrate itself with an invented answer | 08-08 |
| D20 | **The costume metaphor is off the site** | The garment captions (JACKET · CiteVyn, VEST · SaafSaans) and the GROUND/CLOTH/THREAD swatch tags are deleted. The swatches printed 21 lines of type describing the site's own paint, in the highest-value space on every plate, and two of three lines were identical by rule. The corner now carries the plate's **state**, which is the fact the audience came for | 08-08 |
| D17 | **The certificate clause is off the site.** The plate states only what the endpoint returns | The spec read the certificate from Fly's record, not from the endpoint. Asked today, the endpoint presents no certificate at all, so "the certificate is valid" cannot be confirmed from where a visitor stands. What *is* confirmable stayed: the name resolves and the connection is accepted | 08-08 |

### D12 — the install list, not yet executed

```
mattpocock/skills       domain-modeling, grilling, grill-with-docs        ← highest value
project-doc-skills      architecture-and-decisions  (from dist/*.skill)  ← aimed at our problem
project-doc-skills      doc-critic
obra/superpowers        verification-before-completion, test-driven-development,
                        systematic-debugging, requesting-code-review,
                        receiving-code-review, dispatching-parallel-agents
addyosmani/agent-skills code-review-and-quality, performance-optimization
addyosmani/web-quality  performance
ui-ux-pro-max repo      design-review agent (adapted, MIT) — the independent UI lens
```

Three traps:

1. **Do not install both TDD skills.** obra's and addyosmani's share a directory name; the
   second overwrites the first. obra wins on mandatory watch-it-fail.
2. **Install `project-doc-skills` from `dist/*.skill`, never by copying `skills/<name>/`.**
   The source directories are build *inputs* — `house-style.md`, `project-profile.md`, and
   `verify.py` are copied in by `build-skills.sh`. Copying the source yields dangling refs.
3. **`grill-with-docs` is a 245-byte pointer.** Its whole body is *"Run a `/grilling` session,
   using the `/domain-modeling` skill."* Install all three or it points at nothing. Also: the
   "docs" it makes are ADRs and a glossary — it does **not** fetch documentation.

### Conditional — install when the trigger fires

| Skill | Install when |
|---|---|
| `graphify` | Pointed at `docs/` and prose, never `src/` — there is no `.astro` tree-sitter grammar, so the free AST path barely applies and everything falls to the token-costing path |
| `claude-obsidian` | Only if memory living **outside** the site repo is acceptable. Its rule: *"The checkout contains the product. It is not your knowledge vault."* |
| `project-faq` | If a Q&A page is wanted. It emits generated tabbed HTML, a foreign artifact next to Astro |
| `learning-track` | If the writing section becomes a teaching series. It prescribes an 8-critic loop — heavy for one person |
| `onboarding-companion` | If someone else contributes. Doubles as a re-entry doc after a long gap |
| `operations-runbook` | If a case study covers a running service |

### Uncovered capability

**Grounding answers in fetched documentation** has no public skill I could find. `grill-with-docs`
looked like it and is not. Worth watching, since the NarraTwin avatar will need exactly this.

---

## Open — blocked on the owner

| # | Item | What is needed |
|---|---|---|
| ~~O1~~ | **Approved 08-07 and done.** Remaining contrast work is DEF-5 |
| ~~O2~~ | **CLOSED — the repo has been public since D53 (08-09).** This row was stale: it still read "blocked for me — run it yourself" a day after the flip actually happened. Found and fixed as ledger hygiene (item 9, `hero-animations-wordmark`) | — |
| ~~O3~~ | **CLOSED 08-09 by D48** — the owner chose adoption as-is; shipped with the baked background stripped (approved). See D48 for scope and the recorded trade-offs |

## Open — mine to do

| # | Open item | Note |
|---|---|---|
| ~~O-HERO~~ | **RESOLVED — `hero-animations-wordmark`, per the recommendation `docs/ACTION-PLAN.md:40` item 1.2 already made and never executed.** The refusal card left the hero entirely rather than being captioned in place: it now sits on the CiteVyn plate (`id="citevyn"`) as a second, compact `Artefact` card, where the visitor has already been told what question CiteVyn answers, so "Refused" reads as the feature it is rather than a system that looks broken. Same real data, only its position moved — nothing invented, nothing deleted. The hero's card slot is replaced with a pull-quote (the demo/release line) and a `CapStrip` proof strip of real counted figures (6 systems built, 4 release gates, 52/52 golden cases) — the first thing a visitor reads is evidence of scale, never a "no." *Was:* **The hero refusal card does not state its own conclusion** — the full-page persona critique had scored it as the visit's peak, which argued for keep-and-caption over relocate. The approved implementation plan (`resilient-hatching-peacock.md`, item 8) settled it: execute the relocate fix already recommended in `docs/ACTION-PLAN.md`, rather than re-deferring or inventing a third option | — |
| ~~O-OVERVIEW~~ | **BUILT 08-09.** Plate Nº 01, the contact sheet: six rows, each with what the system does, the gate it runs against itself, its state and one hard number. The gate column is the argument — Phase 0 found four of the six systems ARE gates. *Was:* **The bird's-eye overview was never built, and was never named as a finding** | The critique snapshot mentions "overview"/"summary" **zero times** while finding every symptom. It is D1 (07 Aug), unexecuted. Shape agreed: a contact-sheet plate after the hero, one row per system — what it does, its state, one hard number |
| ~~O-30SEC~~ | **CLOSED 08-11 — both halves refuted by measurement, and both numbers in this row were wrong.** The `Seeking` half is fixed: **one** title ships (*"Senior / Principal — AI Platform Engineering, with evaluation and release governance"*), and `index.astro:71`'s own comment describes the fix in the past tense. The type-ratio half was never accurate: measured on the served build at 1440px with `getComputedStyle`, the name renders at **31.68px** and the h1 at **59.04px** — a ratio of **1 : 1.86**. This row said **18.24px against 96px**, i.e. 1 : 5.3. **18.24px is the lede *paragraph*, not the name**, and **96px is the old `clamp(3rem, 6.8vw, 6rem)` value deleted long ago** — `global.css:289` carries the replacement *and* a comment explaining why. A row that misreads its own file by 1.74× on one number and quotes a deleted value for the other would have sent the next reader to fix a problem that no longer has that shape | — |
| ~~O-SKILLGAP~~ | **CLOSED 08-11 — refuted, and self-contradicted.** Six positioning/messaging skills are installed and locked: `brand-positioning`, `brand-messaging`, `brand-story`, `brand-voice`, `brand-strategist`, `brandkit` (`ls .claude/skills`, `skills-lock.json`). **D34 in this same file records installing them on 08-09**, along with the GitHub searches and why `coreyhaines31/marketingskills` and `wonjyou` were rejected — which is the second half of what this row asked for. `brand-positioning` was then actually used, for `docs/positioning-phase0.md` (D40). The row survived two days after the file already contained its refutation | — |


| # | Item | Blocked by |
|---|---|---|
| M1 | ~~Static-render defect~~ **done** · now DEF-5, the opacity ramp | Nothing |
| ~~M2~~ | **Done 08-11.** All 14 D12-named skills present under `.claude/skills/` and 50 locked in `skills-lock.json`. `architecture-and-decisions` and `doc-critic` are absent from the lock by design — D12 trap 2 says install them from `dist/*.skill`. **One genuine remainder, tracked separately: the `ui-ux-pro-max` design-review agent** — `.claude/agents/` does not exist. D68 records the decision not to adopt that skill (it contradicts `impeccable`, which owns the brief; a same-model lens is not independence), with `codex exec --sandbox read-only` named in its place | — |
| ~~M3~~ | **Done** — `tests/visual-baselines.spec.js` shipped in PR #4, with committed snapshots under `tests/visual-baselines.spec.js-snapshots/`. This row had gone stale after the fact; found and struck as ledger hygiene (item 9, `hero-animations-wordmark`) | — |
| M4 | **Half done, and the halves were never distinguished.** CI: **done** — `.github/workflows/gates.yml` is tracked, two jobs, and branch protection now requires both (D71). Git hook: **done** — `.githooks/pre-commit` is tracked and `core.hooksPath` points at it. **Still open: the agent-side `Stop` hook is NOT tracked.** It exists only in `.claude/settings.local.json`, which `.gitignore:28` excludes, so it lives on one laptop and a fresh clone reproduces nothing. Compounding it, **`.gitignore:25` asserts that `.claude/settings.json` "is tracked and carries the shared settings file" — that file does not exist** | Nothing |
| ~~M5~~ | **Done — and re-verified by execution 08-12, after being wrongly marked "unauditable".** M5's item text was *"Deep links `?at=` work without JavaScript"*. It was **recovered from git history in one command** (`git log --all -S"| M5 |" -- docs/STATUS.md`, then `git show <sha>:docs/STATUS.md`), which is what should have happened instead of declaring the row untestable. **Measured on the served build at 1440px, JavaScript ON vs OFF: identical.** Both land `scrollY 1800`, `#citevyn` top at `0px`, in view, `.plate-copy` opacity `1`. The fragment is resolved by the browser before any script runs, and DEF-1's fallback keeps the content visible with JS off. The lesson stands from the other direction: a row whose item text has been destroyed is recoverable from git — "unauditable" was a failure to look, not a property of the row | — |
| ~~M6~~ | **CLOSED 08-12 on a fresh measurement — no measurable effect either way.** **Correction to my own handling:** on 08-11 the owner was offered "strike M6 citing DEF-3" vs "re-measure, then decide" and **chose re-measure**; I then struck it on DEF-3's number anyway. That overrode a recorded decision and is corrected here. The re-measurement is `tests/preload-ab.mjs` — 7 runs per arm, both viewports, fresh context per run, LCP read from `PerformanceObserver`, arms differing by exactly one injected `<link rel=preload>` so nothing is rebuilt between them. **Run against production** (`https://stackclimb.com`), because a localhost run was measured first and rejected as meaningless: LCP there is 40–56 ms with no network latency, which is precisely the condition where a preload has nothing to save. Production result — desktop **A 348 ms vs B 364 ms (+16)**, mobile **A 368 ms vs B 348 ms (−20)**. **The two deltas point in opposite directions and both are dwarfed by run spread (204 ms and 140 ms).** That is the signature of noise, not effect. **This does not reproduce DEF-3's finding** (+44 ms desktop, +96 ms mobile) — and it need not: DEF-3 measured Astro 5, a different hero and a different LCP element. Today's build is simply indifferent to the preload. **Verdict: not worth adding.** Re-opening needs a measurement showing a real gain, not an argument | — |
| ~~M7~~ | **Done 08-09** — grain 38KB WebP, og 72KB regenerated (DEF-23/35). `/impeccable optimize` added `public/_headers`: `/_astro/*` now `immutable, max-age=1y` (hashed URLs cannot go stale); unhashed files deliberately excluded per DEF-24 | — |
| ~~M8~~ | **Done 08-11 (D65)** — five evidence files written, every number re-derived at HEAD with its command and SHA. Four inherited figures refuted in the process | — |
| M9 | NarraTwin avatar as the site's representative | NarraTwin's own No-Go |

---

## Defects found and not yet fixed

| ID | Severity | What | Evidence |
|---|---|---|---|
| ~~DEF-50~~ | — | **FIXED 08-09.** A personal mobile phone number and LinkedIn QR code, rendered as pixels inside 4 visiting-card PNGs, were committed to `main` in commit `c2a5933` ("Design doc: launch scope") on 08-07 and later removed from the working tree by `f2feb4c` ("Never track brand candidates") — but the blobs remained fully retrievable from git history, and neither `gitleaks` nor `tests/no-pii.mjs` caught it because both scan text, not pixels. Worse: the same commit was an ancestor of every one of the repo's 28 GitHub `refs/pull/N/head` refs, which GitHub retains independently of branches and which a local history rewrite + force-push cannot remove. **Fix:** rewrote local history with `git filter-repo --invert-paths --path docs/brand/candidates` (verified: old commit hash, old blob hash, and the path itself are unresolvable in the rewritten repo); renamed the GitHub repo to `stackclimb-website-archived-20260809` (kept private, not deleted — reversible); created a fresh `imrohitagrawal/stackclimb-website` and pushed the cleaned history there, which starts with zero PR refs. No branch protection or repo secrets existed to re-configure. Everyday version: the old repo was a filing cabinet with a folder that had been emptied but the drawer still had a false bottom holding a copy — the fix was a new cabinet, not a better lock on the old one | `git cat-file -t <old-commit>` and `git cat-file -t <old-blob>` both fail post-rewrite; fresh `git clone --bare` of the new repo shows `git rev-list --objects --all \| grep brand/candidates` empty and `git for-each-ref` shows only `refs/heads/main`; `gitleaks detect` clean on the fresh clone, 85 commits |
| ~~DEF-1~~ | — | **FIXED.** Plates now paint their own ground in static HTML; JS reclaims it via `.js-ground` before first paint | No-JS plate bg now `rgb(181,172,156)` linen vs ink text = 7.1:1. Cross-fade unchanged |
| ~~DEF-5~~ | — | **FIXED 08-08 by the value ladder.** Desktop and mobile now scan clean, 18/18. Was: **Desktop 4 plates / 22 nodes, Mobile 3 plates / 16 nodes** — `#private` passes at 390px and fails at 1440px. The `DESIGN.md` opacity ramp (prose 88%, labels 62%) was tuned on ink navy and fails on lighter grounds | axe per-plate scan, both projects |
| ~~DEF-6~~ | — | **REFUTED 08-09, and replaced by something bigger.** Original wording kept below. `.js-ground` gated exactly one CSS rule — `.js-ground .plate { background: transparent }` — and commit `75ea9a7` (the DEF-30 fix, 08-08) deleted that rule. The class had no consumer, so it could not fail open. Verified: `grep -c js-ground dist/_astro/*.css` → **0**. The inline script and the dead observer that fed it are now deleted. **RCA-002's proposed one-line fix would have been a literal no-op.** What the investigation found instead is **DEF-38**, which is worse and was live. *Was: "The DEF-1 fix fails open. `.js-ground` is set by an inline script that always succeeds, while `plates.js` — which does the repainting — can throw independently. JS on + module dead = 1.03:1, 14 nodes."* | `grep -c js-ground dist/_astro/*.css` → 0; RCA-003 |
| ~~DEF-38~~ | **HIGH** | **The fixed nav has no ground, and is unreadable on 46% of the page. Live on `stackclimb.com`.** `.site-nav` was `position: fixed` with **no background at all** — bone text floating over whatever plate scrolled beneath, including the near-white `#private` paper plate added by the value ladder (D18). Measured in **rendered pixels**: **22 of 48 desktop scroll positions and 26 of 78 mobile below 4.5:1, worst 1.00:1** — foreground and background the same value, the link invisible. **All three existing contrast checks were structurally blind to it**: `dod.spec.js` scopes axe to `#${plateId}` (DEF-14's blind spot, still open), `boundary-check.mjs` samples plate seams only, and axe's contrast rule reads the DOM, where a background-less fixed bar computes to `rgba(0,0,0,0)`. So the suite read 18/18 and the seam gate read "AA ok, worst 7.56:1" over an invisible nav. **Fixed:** the bar carries its own `--nav-ground`, and the theme-reactive colour rule is gone. **0 of 48 and 0 of 78 after, worst 15.60:1.** Held by `tests/nav-contrast.mjs`, proved to bite by three watched mutations | RCA-003. Pixel sweep at 120px steps, both viewports |
| ~~DEF-7~~ | — | **RESOLVED 08-09 by the owner's redeploy.** Verified from here, each claim by a command: `https://saafsaans.stackclimb.com/` returns 200 in 0.28s serving the real app ("SaafSaans — Anand Vihar"); `flyctl status` shows machine `80e449c6253118`, version **9**, state `started`, 1/1 checks passing; `flyctl config show` confirms scale-to-zero is back (`auto_stop: suspend`, `min_machines_running: 0`). The site's outage disclosure is replaced with the suspend-and-wake fact plus the outage dates — the voice rule stands: SaafSaans is *deployed*, never "live". The crash cause of 21 July remains unverified (log retention expired before anyone could read them); recorded so the "why" is not mistaken for known. *Was:* the Fly app had ZERO machines and the site disclosed the outage | `curl` ×2, `flyctl status`, `flyctl config show`, 08-09 |
| ~~DEF-8~~ | — | **FIXED 08-10 (pr3-no-js-resilience).** New `src/styles/print.css`, imported once in `Layout.astro`, ports cv.css's print treatment to the other seven plates: ink text, transparent ground, `body::before` hidden, links resolved to their href in the margin. Scoped `.plate:not(.cv)` so cv.css stays the sole authority for `/cv`. Found while building it: `.links`/`.ctas` are flex rows on screen and a revealed GitHub blob URL broke their layout under print until stacked to one destination per line; the external-link icon's `::after` already claimed that pseudo-element at higher specificity and clipped the revealed href to 0.58em until every property, not just `content`, carried `!important` — both found by looking at a print proof, not by assuming. **Review found a gap 08-10: `PLATES` covered 6 of 7 plate ids and silently omitted `overview`, against the test plan's own "for each of the 7 plate ids." Fixed same-commit** — `overview` added, now 7/7 | `tests/print.spec.js`, 17 assertions across all 7 plates plus `/cv` unaffected; mutation-proved (disabling the `.plate:not(.cv)` rule turns assertions red) |
| ~~DEF-9~~ | — | **FIXED 08-09.** Canonical and `og:url` are derived from `Astro.url` against `site`, so a page cannot forget to set one or set it wrong. **A second defect was found inside the fix and closed with it:** under `build.format: 'file'` the build-time pathname is `/approach.html` while the URL that serves — and the one the sitemap emits — is `/approach`, so the naive fix would have named two URLs for one page. Caught by diffing canonical against sitemap on a throwaway page, not by reading. The 404 declares `noindex` and no canonical at all | Mutation: hard-code the canonical again and the gate reports *"two routes share a canonical"* |
| ~~DEF-10~~ | — | **FIXED 08-10, both files.** `dod.spec.js` was already fixed 08-09. `visual-walkthrough.mjs:14`'s hand-typed `PLATES` array was the same defect in a second file, never touched by that fix — and it was already silently wrong: `overview` had zero coverage, and `work` (renamed `citevyn` by DEF-34) meant every `?at=work` screenshot captured whatever the page fell back to. Now derived from `page.$$eval('.plate[id]', ...)` after the browser launches, with a non-empty-list guard. Proved by adding a throwaway plate to `index.astro`, re-running with zero edits to the script, and seeing it captured (`09 plate-probe-temp-desktop`) — then reverting | `node tests/visual-walkthrough.mjs`: before fix, 7 plates captured including a bogus `work`, `overview` absent; after fix, all 8 real plate ids captured; throwaway-plate mutation captured with no script edit |
| ~~DEF-11~~ | — | **FIXED 08-09, both halves.** CI already built first (gates.yml:94, 08-09 overnight); the LOCAL config now does too — `webServer.command` runs `npm run build &&`, and `reuseExistingServer` is `false` so a stale server is a loud port conflict, never a silent reuse. Proven: renaming `#contact` in the source with no manual rebuild turns the suite red, where before it stayed green against stale `dist/` | Mutation run 08-09 |
| ~~DEF-12~~ | — | **REFUTED 08-09 as recorded; residual slack closed.** `contact.spec.js`'s `MIN_PLATES` floor (added `d7787f3`, 08-08) already turned plate deletion red — the audit re-ran the recorded mutation and got 8 failed. But the floor sat at **7 while 8 plates ship**, so the whole `#private` plate — the one plate nothing links to — deleted silently with 42 green. Floor now 8, with the bump-on-add rule written beside it. *Was:* deleting 6 of 7 plates left the suite green | Mutation audit, 08-09 |
| ~~DEF-13~~ | — | **CONFIRMED by mutation (a transparent ring passed 42/42), then FIXED 08-09.** The style-reading test is deleted; `tests/focus-visible.spec.js` Tabs through the page like a keyboard user and requires the RENDERED PIXELS around each stop to differ, focused vs blurred — `element.focus()` does not trigger `:focus-visible`, a real Tab press does. Transparent ring mutation now fails 5 of 25 stops on `/` (the other stops carry a second real indicator). What it does not prove, said out loud: a ring that changes pixels at poor contrast still passes; ring contrast is nav-contrast's job on the nav and open elsewhere | Mutation red 08-09 |
| ~~DEF-14~~ | — | **CONFIRMED by mutation, FIXED 08-09.** `tests/chrome-a11y.spec.js` runs one unscoped axe pass per route with `.plate` excluded, so nav, skip link and footer are covered exactly once between the two files. The planted alt-less `<img>` + empty link now fail 2 tests. **Mutation nuance for the next auditor:** the ledger's original `href="/x"` plant fails `links.spec.js` on a 404 — red for the wrong reason; use a resolvable `href="/cv"` to isolate the a11y hole | Mutation red 08-09 |
| ~~DEF-15~~ | — | **CONFIRMED by mutation (a 4th viewport failed "no viewports checked"), FIXED 08-09.** Now `toBeGreaterThanOrEqual(3)` — a floor. Dropping to two widths goes red; adding a fourth stays green | Mutation audit 08-09 |
| ~~DEF-16~~ | — | **CONFIRMED by mutation — flattening made the seam gate GREENER (15.60 → 17.70) — and FIXED 08-09 by `tests/palette-ladder.spec.js`** (see DEF-48, which is this defect found shipped rather than hypothesised). Contrast floors reward flattening; the ladder gate asserts distinctness instead | Mutation red 08-09 |
| ~~DEF-17~~ | — | **FIXED 08-09 — pinned to `1.62.1`, no caret. Recorded harm was overstated:** CI runs `npm ci`, which installs from the lockfile where all four playwright packages were already pinned, and no `toHaveScreenshot` baselines exist anywhere to invalidate. The caret was real drift-risk for a bare local `npm install`; that is what the pin closes | `package.json:19`; audit 08-09 |
| ~~DEF-18~~ | — | **WRONG AS LOCATED, and now moot.** Neither element is in the footer: `dl .row dt` and `.site-nav .brand em` both live in `#top`, and they only measured 1.51:1 / 2.79:1 while the linen ground was painted — the same root cause as DEF-30. The real footer measured 0 violations at both viewports. Fixed by DEF-30's fix | Assessment B, per-plate axe with each plate in view |
| DEF-19 | **Low — LATENT, downgraded from HIGH** | **Re-measured 08-09. Nothing is failing, because no CSP exists.** `curl -sSI https://stackclimb.com/` returns no `content-security-policy`; there is no `_headers` file on disk or in history. This is a defect that *arrives* the day someone adds a CSP, not one running in production. Its damage figure was also stale: "1.03:1" is DEF-30's number, and DEF-30 was fixed 08-08. Serving the built site under four policies (none · head-hash only · both blocked · both allowed) gives **byte-identical** boundary results — worst 7.56:1, AA ok, in all four. **The one true part survives:** a hand-maintained script hash goes stale on every edit. That trap now has nowhere to live — the inline script it referred to was deleted with DEF-6. When a CSP is added, use Astro's own `experimental.csp`, which derives hashes from the shipped bytes at build time; never hand-write them | Four policies served, `boundary-check.mjs` against each |
| ~~DEF-20~~ | — | **FIXED 08-10, merged current `main` (PRs #1–#4) into the branch first — conflicts in `package.json`, `package-lock.json`, `docs/STATUS.md` resolved keeping both sides' real content (PII-PDF-scan deps `mammoth`/`pdf-parse` plus the astro bump; lockfile regenerated with `npm install` after taking main's base).** `astro` bumped `^5.12.0 → ^7.2.0` (resolved `7.2.0`). `@astrojs/sitemap@3.7.3` needed no bump. `npm audit` before: astro direct, high, 8 advisories. After: 0 astro findings — the one remaining high is `sharp`/libvips (CVE-2026-33327/28, 35590/91), transitive image-processing, out of scope. Quick supply-chain check on the bump's new transitive deps (`am-i-vibing`, `process-ancestry`, and everything else newly added to the lockfile): only `esbuild`, `fsevents`, `sharp` carry install scripts — all expected native-build packages, nothing new; no network calls or unexpected file writes found in `am-i-vibing`/`process-ancestry` source, both only read `process.env`/`ps` output. **Structural bug found and fixed: `astro preview` silently daemonizes under an AI agent.** Astro 7.2's new `am-i-vibing` dependency (`node_modules/astro/dist/cli/agent.js`) detects agent env vars (`CLAUDECODE`, `CURSOR_TRACE_ID`, `REPL_ID`, etc.) and `astro preview` (`node_modules/astro/dist/cli/preview/index.js:40-58`) responds by backgrounding itself and returning immediately — reproduced: with `CLAUDECODE=1`, `npx astro preview` exits in 738ms while a detached process keeps the port bound (verified via `ps`/`lsof`). That breaks Playwright's `webServer`, which needs the launched command to stay in the foreground; it does not affect real GitHub Actions CI, which sets none of these vars. Fix: `astro preview`'s own opt-out is the env var `ASTRO_PREVIEW_BACKGROUND` (same file, line 40 — `!process.env.ASTRO_PREVIEW_BACKGROUND && isRunByAgent()`); wired into `playwright.config.js`'s `webServer.env` so every future test run gets it automatically, without touching `npm run dev`/`preview`. **Tested both ways, not assumed:** full gate suite (`npm ci`, `npm run build`, `no-pii.mjs` + self-test, `file-budget.mjs` + self-test, `npx playwright test`, `boundary-check.mjs`, `nav-contrast.mjs`) run twice — once with `CLAUDECODE=1` ambient (this session's real shell), once with every Claude/agent env var explicitly stripped (`env -u CLAUDECODE -u AI_AGENT …`, simulating a human shell) — identical results both times: 118 Playwright specs passed, 2 skipped (coarse-pointer, by design), boundary-check worst seam 7.56:1, nav-contrast worst 6.26:1. No leftover daemonized `astro preview` process after either run (`ps`/`lsof` clean) | `npm audit --json` before/after; `ps`/`lsof` timing proof of the daemonize bug; two full gate-suite runs (agent env present / stripped); `playwright.config.js` diff; branch `pr5-astro-major-bump` merged with current `main` |
| ~~DEF-22~~ | — | **STALE — re-measured 08-09 and closed without the fix it prescribed.** Lighthouse today reports **zero render-blocking resources** and **zero opportunities with any saving**: mobile 100 / LCP 1.6s, desktop 100 / 0.4s, 154 KiB total. The 2.4s LCP and 98 score it describes belonged to a build that no longer exists (the value ladder, asset diet and hero work moved everything). Inlining the stylesheet now would trade a cacheable file for zero measured gain. *Was:* render-blocking CSS gates font discovery; inlining takes mobile LCP 2.4→1.4s | Lighthouse 13.4.1, 3-run medians, 08-09 |
| ~~DEF-23~~ | — | **FIXED 08-09.** `paint-grain.webp`, grayscale q95: 166,280 → **37,650 bytes**. Source-pixel max error 6/255; the RENDERED page — the claim that matters, at opacity 0.5 under overlay blend — diffs **0 pixels** against the PNG build, measured by intercepting the request and comparing screenshots. The rename also sidesteps DEF-24's immutable-cache trap for this file: a new URL cannot serve a stale cache | Rendered diff, 0/255 max error |
| **DEF-24** | **Low — narrowed 08-09** | `og.png` and `favicon.svg` still ship unhashed from `/public`. The grain left the list by becoming `paint-grain.webp` (new URL). `og.png` deliberately keeps its stable name — share crawlers hold the URL — so IT is the file this defect now describes: a future og change needs either a cache purge or a versioned name plus updated `og:image` | Build output |
| **DEF-21** | **Medium** | `scroll-behavior: smooth` (`global.css:36` (this row said `:18`; corrected 08-11 by `grep -n scroll-behavior`)) makes every fragment link a ~3-second animation. `?at=` is **not** vestigial — it sets `no-anim` for an instant jump. Migrating to `#` needs a settle wait or every deep-link screenshot captures mid-scroll | Synthesizer, timed |
| ~~DEF-25~~ | — | **FIXED.** The SaafSaans plate claimed a "three-hour build" started at the event. Nothing in `saaf-saans` records a duration. The three commits on 18 July land 14:17, 14:49 and 15:21 — **inside** the event, which ran 09:00–17:30 IST. The first is titled "existing UI baseline"; the linked case study's timeline row reads "Before 18 Jul — Built for a hackathon as a 4-tab Streamlit app. Did not place." Copy now says the project was **entered** at the event as a four-tab Streamlit app that already existed | `git log` in `saaf-saans` (master); `docs/CASE-STUDY.md:32` |
| ~~DEF-26~~ | — | **FIXED.** The same plate said "the certificate is valid and the address resolves". Half true. The name resolves (`66.241.125.120`) and TCP 443 accepts, but the server closes the connection without presenting a certificate. Copy now states only what a command returns | `nc -z 66.241.125.120 443` succeeds; `openssl s_client` → "unexpected eof while reading" during init |
| ~~DEF-27~~ | — | **FIXED.** Relocation was stated twice in two wordings — hero ledger "Relocation / Open worldwide" and contact ledger "Open to / Global relocation · international travel" — in the change whose commit message said nothing was said twice. The hero row was removed; the contact row carries strictly more (it also states international travel) | `index.astro:52` and `:356` before the fix |
| ~~DEF-28~~ | — | **FIXED.** `tests/contact.spec.js` had **nine** holes and could not fail for what its own header claimed. Deleting the whole contact plate passed; `body { display: none }` passed; two unlabelled links passed. Rebuilt, with every hole proved closed by a watched mutation | Three reviewers, five holes found only by a different model family |
| ~~DEF-29~~ | — | **FIXED — shipped in `4f9efbe` (08-09), ledger was stale.** The location was stated twice; the hero ledger's `Base` row is now gone, and only the contact ledger's `Based — Bengaluru, India · IST (UTC+5:30)` row remains. Same resolution named in the original defect: the row carrying strictly more (D27 precedent) survives. This row was never struck in the same commit as the fix, so the ledger kept reporting it open a day after it shipped. *Was:* the location was stated twice, verbatim, in the hero and contact ledgers | `git log -S"DEF-29" -- src/pages/index.astro` → `4f9efbe`, ancestor of `HEAD`; `grep -n 'Bengaluru' src/pages/index.astro` returns one row (`:475`) |
| ~~DEF-30~~ | — | **FIXED 08-08. Content was invisible at every plate seam, on the live site.** `.js-ground .plate { background: transparent }` meant one ground was painted at a time, but 94% of desktop scroll positions show two plates. Measured: private text on the contact ground at **1.03:1**, narratwin on private at **1.77:1** — the top 468px of a plate unreadable while scrolling past it. Every plate now paints itself; worst seam **7.56:1** | `tests/boundary-check.mjs`, before and after, plus screenshot |
| ~~DEF-31~~ | — | **FIXED 08-08. Two stale numbers were on the CiteVyn plate.** "Tests 361 passing" — the repo has **1,036 test functions across 110 files** at `df8cfc3`. "Release gate 50/50 golden" — the recorded run holds **52** cases. Both came from a README rather than a count | Counted at HEAD in `citevyn` |
| ~~DEF-32~~ | — | **FIXED 08-10, then re-opened and fixed again same day.** Both instances of "21 stations" on the SaafSaans plate were qualified first — prose "across a repo-stated 21 stations" and the `CapStrip` chip "21 stations (repo-stated)" — but review found a third, identical unqualified instance on the `/cv` page: `src/data/cv.js`'s `projects` entry for SaafSaans, rendered verbatim by `cv.astro` with no qualifying note in that section (the page's only "self-reported" note is scoped to Experience, not Independent systems). `cv.js:150` now reads "across a repo-stated 21 stations" too. *Was:* the figure appeared as unqualified site fact on `index.astro` (both instances) and, missed by the first fix, on `cv.js`/`/cv` | `grep -rn '21 stations' src/pages/index.astro src/data/cv.js` shows all three instances carry "repo-stated"; reverting any one drops the match count |
| ~~DEF-33~~ | — | **STALE — re-verdicted 08-09 by the polish pass.** The three `<rect>`s are the garment rail and the two name tags — drawn content, not placeholders; the render shows finished garment bags with tags and honest-absence dashed hems. Whether the bags read as "headstones" is the recorded taste question, not a defect. *Was:* still draws 3 placeholder rectangles | Render inspected at both viewports, 08-09 |
| ~~DEF-36~~ | — | **FIXED — shipped in `e907929` (08-09), ledger was stale.** `Layout.astro:77` used to read *"Built as a work sample — static Astro, self-hosted type, no trackers, no invented claims"*. Deleted, not rewritten; the footer now reads only `© 2026 Rohit Agrawal · Bengaluru`, with a comment recording the DEF-36 rationale. This row was never struck in the same commit as the fix, so the ledger kept reporting HIGH-severity open a day after it shipped. *Was:* the footer told a recruiter the site was an exercise | `git log -S"DEF-36" -- src/layouts/Layout.astro` → `e907929`, ancestor of `HEAD`; `Layout.astro:182-190` today has no "work sample" text |
| ~~DEF-34~~ | — | **FIXED 08-09.** The plate is renamed `#citevyn`, and `Systems` plus the skip link now point at `#overview` — a plural label reaching the contact sheet rather than one project. *Was:* **The nav label and the anchor it points at disagree.** `Systems` and the skip link `Skip to the systems` both target `#work`, and `work` is **CiteVyn's plate id** — so a plural label lands the visitor on one system. `work` is also the odd id out: every other plate is named for its project (`quorum`, `saafsaans`, `narratwin`). Leftover from when `work` was a section. Fixed by D28: nav becomes `WORK → #overview` and the plate is renamed `#citevyn` | Owner found it, 08-09 |
| ~~DEF-35~~ | — | **FIXED 08-09.** Regenerated by screenshotting the REAL hero at 1200×630 @2x — current palette, headline, avatar and the CiteVyn refusal card; nothing drawn by hand, so it cannot drift from the design the way a hand-made card does. 337,549 → **71,977 bytes**, which also retires the "largest file in the build" note: the largest is now this same file at a fifth of its old weight | Screenshot pipeline in the PR |
| ~~DEF-40~~ | Medium | **Every URL returned the home page with HTTP 200 — no 404, no robots.txt, no sitemap.** Cause was a single missing file: Cloudflare Pages treats a build with no top-level `404.html` as a single-page app. There was no `_redirects` and no catch-all rule. **Fixed 08-09** — `src/pages/404.astro`, `public/robots.txt`, `@astrojs/sitemap`, and `build.format: 'file'` decided before `/approach` and `/cv` exist because it fixes how every internal link must be written. Verified against **Cloudflare's own asset router**, both directions: with `404.html` a missing path returns 404 and real routes still serve; remove it and the same path returns 200 with the identical byte count as `/` | `wrangler pages dev`, before and after |
| ~~DEF-41~~ | **HIGH — fixed the day it appeared** | **Every nav link was a bare fragment, so the menu silently did nothing on any page but the home page.** `href="#work"`, `href="#contact"`. With one page that worked; the moment `/cv` shipped, "Systems" and "Contact" pointed at ids that are not on that page — no navigation, no console error, nothing red. **Every existing gate passed them**: they are present, visible, focusable and correctly labelled, and no check asked whether a link *arrives*. Same lesson as DEF-28's cross-model finding — `count()` proves existence, never reachability. Fixed to root-relative, and held by `tests/links.spec.js` | Mutation: revert to `#work` and it reports *"no #work on /cv"* while `/` still passes |
| ~~DEF-8~~ | — | **FIXED 08-10 for the rest of the site — see the row above.** `src/styles/cv.css`'s own print block, described here, is unchanged and remains `/cv`'s sole authority | Playwright `emulateMedia({media:'print'})` proof, before and after |
| ~~DEF-42~~ | — | **FIXED 08-09 — RCA-004, D46.** A native `<details>` menu below 900px; nav concern extracted to `src/styles/nav.css`. Measured before: `Systems`, `CV`, `Contact` at 0×0 on every viewport ≤900px, 14 phone screens with no navigation. After: every destination reachable by activation at 360/390/768/900/1440, JS on **and off**. Held by `tests/nav-reach.spec.js` — hide the menu and 4 tests fail; hide the desktop row, red; make the summary unclickable, red. *Was:* **on a phone the nav reaches nothing** — `.site-nav nav a:not(.chip) { display: none }` below 900px with nothing in its place | RCA-004; three watched mutations |
| ~~DEF-43~~ | — | **FIXED 08-09.** The plate now measures exactly 900px at 1440×900 — the `min-height: 100svh` floor — inside the One-Plate-One-Viewport rule. All cuts are chrome, scoped to `#overview` in `overview.css`: plate pad-block 162→94px, frame pad-block 115→45px, row padding 0.85→0.45rem, and the title moved to the ramp's **documented** `.tight` step. No row content changed. One nuance beyond the recorded correction: padding alone was not quite enough — the correction's own arithmetic left the frame at 990px — so title-step and row-gap joined the padding cuts. *Was:* 1149px in a 900px viewport | Measured 900/900 at 1440×900; render screenshotted |
| ~~DEF-44~~ | **HIGH** | **The plate-seam gate could not fail for the thing it measures. An eighth test that cannot fail.** `tests/boundary-check.mjs` printed `*** below AA ***` and then **exited 0**, so `gates.yml`'s "plate seams stay above AA" step passed regardless of the measurement. It failed for the first time today only because the script **crashed** on a renamed plate id — red for the wrong reason, green for the wrong reason. **`AGENTS.md`'s standing debt cites "Contrast passes on all 7 plates, both viewports" as DONE on the strength of this step**; that was true when measured by hand on 08-08 and was never actually enforced. Also fixed with it: the hand-typed `IDS` list (same class as DEF-10, third file — renaming `work` to `citevyn` crashed it, and merely *deleting* an id would have silently measured six seams and reported AA ok), a `Math.min` of an empty array returning `Infinity` and printing "AA ok" over zero rows, and `--legacy`, which selected `.js-ground .plate` — a class deleted with DEF-6, so the flag had been silently measuring the FIXED page while labelling it "BEFORE" | Mutation: set `.plate` background to transparent → exit 1, `worst 1.03:1 at "private → contact"`. Healthy → exit 0 |
| ~~DEF-45~~ | **HIGH — shipped and fixed the same day** | **The skip link stopped being a bypass mechanism.** Changed to `href="/#overview"` in the Phase 1 package; on `/cv` that id does not exist, so the control **navigated the reader off the page**. Lighthouse scored `skip-link` **0** on `/cv` and it was the entire 2-point accessibility gap (98 vs 100). **Neither existing gate could see it:** axe has no skip-link rule, and `tests/links.spec.js` resolves `/#overview` by loading `/` and finding `#overview` there — which it does. The rule a skip link must obey is *targets the CURRENT page*, which the resolve test cannot express. Now `href="#main"` with `<main id="main">` on every page, and `links.spec.js` encodes the rule | Mutation: revert it and the resolve test still **passes** while the new one fails on both routes |
| ~~DEF-46~~ | — | **FIXED by changing the approach, not by a fifth patch.** The gate measured the resting state only, so a low-contrast `:hover` or `:focus-visible` colour passed it. Four geometry-based fixes each produced a new false positive — sampling above the box read the bar behind the ochre chip (1.16:1 for a ~6:1 pair); bounding corners read the focus ring, because the chip is a 999px pill; a fixed outside gap landed on the ring again at `outline-offset: 3px`; compositing the element's own background reported none while the same probe read it as ochre. **All four shared one false assumption: that the backdrop can be located by geometry.** Replaced with `tests/lib/rendered-contrast.mjs`, which renders the bar **three times** — as-is, with the glyph fill transparent, and with the glyph fill set to a probe colour — and reads the answer off the pixels. B is the exact backdrop under every glyph; M recovers per-pixel coverage so anti-aliasing is handled and the foreground is read from the core of a stroke. Geometry never enters it, so pills, outlines, gradients, blend modes and opacity are all correct by construction. `-webkit-text-fill-color`, not `color`, because the chip's border is `currentColor` and would otherwise vanish and be measured as text | Five mutations, all red: nav ground removed 22/28 positions at 1.02:1 · `opacity: 0.2` 60/96 at 1.35:1 · low-contrast `:hover` 1.02:1 · low-contrast `:focus-visible` 1.22:1 · links hidden → denominator failure |
| ~~DEF-2~~ | — | **FIXED 08-10 (pr3-no-js-resilience).** `?at=<plate>` was always JS-only, but every plate already carried a real `id` — `#<plate-id>` resolves natively before any script runs, no server infrastructure needed (none exists: no `functions/` dir, `astro.config.mjs` is pure `output:'static'`). `plates.js` extended to also read `location.hash` so JS-enabled visitors get the same instant jump `?at=` always gave. `AGENTS.md`'s documented interface corrected from `?at=` to `#id` — `?at=` was never public-facing (appears only in this repo's own docs/tooling, confirmed by grep) | `tests/no-js-nav.spec.js`: `javaScriptEnabled:false`, `/#citevyn` lands the plate (boundingBox y ≈0), `/?at=citevyn` does not (y >500); mutation-proved (stripping the `id` turns both red) |
| ~~DEF-47~~ | — | **Found and FIXED 08-09. `global.css` stood at 505 lines against its grandfathered ceiling of 500** — and the previous handoff recorded it at 498, so the breach predates that session's own warning not to add to the file. Fixed structurally: the whole nav concern moved to `src/styles/nav.css` (505 → 433), and the prose ceiling became a gate — `tests/file-budget.mjs`, in CI, population derived from `git ls-files` so a new file cannot opt out, exceptions shrink-only. Proven to bite: +70 pad lines → exit 1; `--self-test` proves both directions | `wc -l`; mutation run in the PR |
| ~~DEF-49~~ | — | **Found and FIXED 08-09 by `/impeccable optimize`. Desktop CLS was a stable 0.123 — Lighthouse 96 — introduced hours earlier by PR #21.** Removing the hero's `<br>` let the fallback serif (local Didot, 4.2% wider than Bodoni Moda) wrap the h1 to five lines where the loaded font takes four at 1350px: a 54px reflow of the entire hero on every load. Two-stage fix: a metric-tuned `Bodoni Fallback` face (`local('Times New Roman')` at `size-adjust: 102.7%`, matching advance widths to 0.1% — no vertical overrides needed because line-heights are explicit), and the tag-clearance padding moved from a runtime `.plate:has(.plate-state)` selector to a build-time `with-state` class (the `:has()` was investigated first and refuted as the cause, but a fact the template knows should never be re-derived mid-parse). After: desktop 100 / CLS 0.005–0, mobile median 100 / 1.6s over three runs. **The regression survived six green CI runs because no gate measures CLS** — recorded here as the case for a Lighthouse assertion when deployment moves into CI (approach C) | Frame-by-frame geometry log; `/tmp` traces in the PR |
| ~~DEF-48~~ | — | **Found by the 08-09 mutation audit and FIXED the same day. `palette.css` styled `#work` for a full day after DEF-34 renamed the plate to `citevyn`.** The selector matched zero elements, CiteVyn fell back to the navy default, and FOUR of eight plates painted the same ground — the exact flattening the palette file exists to prevent. Every gate stayed green: contrast gates assert floors, and flattening **raises** floors (the seam gate got GREENER under DEF-16's mutation, 15.60 → 17.70). Now held by `tests/palette-ladder.spec.js`: every built `--ground` rule must land on a real plate and render its own value, and declared grounds must be pairwise distinct. Both mutations red. Its first version derived the expected count from the same artifact the mutation edits and passed the flattening it existed to catch — recorded because self-referential denominators are exactly the DEF-16 trap wearing a new coat | `grep '#work' src/styles/palette.css`; rendered grounds measured before and after |
| ~~DEF-3~~ | — | **KILLED. Preloading fonts made LCP *worse* in every configuration measured** — desktop +44ms, mobile +96ms. `font-display: swap` paints text instantly in fallback; a preload lands inside the block period and makes the browser wait | 7-run medians, A/B |
| ~~DEF-4~~ | — | **FIXED 08-10 — a floor, not full-route coverage.** `dod.spec.js`'s "Evidence capture" block produced screenshots for a human to look at; it never compared a pixel. `tests/visual-baselines.spec.js` now asserts `toHaveScreenshot` on the home page's `.site-nav` and every `.plate`, at the three widths `dod.spec.js`'s own "no horizontal scroll" test already uses (390/768/1440) — covers the nav and plate boundaries, not every route. **Baselines were not hand-generated on a laptop**: `toHaveScreenshot` bakes `process.platform` into the filename, so a macOS-made baseline would not even be the file CI looks for. They come from a manual `workflow_dispatch` run of `gates.yml`'s new `update_visual_baselines` input, on the same `ubuntu-latest`/chromium image the gate runs on, and the committed PNGs are that run's artifact, unmodified. Proved both directions: the real CI gate (PR pull_request run) is green against the committed baselines; a deliberate nav-padding change (0.8rem wider) turned 12 of the new tests red with a pixel diff attached, then green again after the revert | CI run `31334879007` generated the baselines; PR pull_request run `31335046076` green against them; mutation run `31335206653` red (`nav at 390/768/1440px`, `plate boundaries at 390/768/1440px`, both projects), reverted in the next commit |
| ~~DEF-39~~ | **HIGH** | **The CI gate had never run on a pull request, and failed the first time it did.** `gates.yml` grants `contents: read` only; `gitleaks-action` calls `GET /pulls/{n}/commits` on a `pull_request` event and gets **403 Resource not accessible by integration**. Every green run on record — including `31276450288`, cited in this ledger as proof — was a **push** to `main`. **The path branch protection would gate on was the one path never exercised.** Worse: gitleaks ran *before* the PII step, so the job died before `tests/no-pii.mjs` ever executed — the gate D39 exists to enforce silently did not run. Fixed by adding `pull-requests: read` and moving the PII check first, where the workflow's own header comment already said the cheap owner-protecting checks belonged | Run `31278058454`, job `secrets and personal data`, failed in 8s on PR #1 |
| ~~DEF-37~~ | — | **FIXED 08-09, round 1. Round-2 review (five reviewers, one cross-model) found the first fix incomplete in six ways — all fixed 08-10, same commit.** The PII gate can now see inside `.pdf` and `.docx`. Added `tests/lib/extract-text.mjs` (dispatches `.pdf` to `pdf-parse`, `.docx` to `mammoth` plus a direct read of header/footer/footnote/endnote XML, everything else to `readFileSync(utf8)`) and wired `tests/no-pii.mjs`'s `scan()` to call it. **What round 1 got wrong, and the fix:** (1) `gates.yml`'s "secrets and personal data" job never ran `npm ci`, so once this file started importing `pdf-parse`/`mammoth` the job crashed with `ERR_MODULE_NOT_FOUND` before scanning anything — the PII gate was permanently red. Added `npm ci` to that job. (2) The self-test's mutation proof didn't bite: its PDF/DOCX fixtures were uncompressed, so reverting the extraction to raw `buffer.toString('utf8')` still "worked" because the planted digits were visible as raw bytes. Fixtures are now compressed (PDF: `/FlateDecode`; DOCX: zip `DEFLATE`), so only real extraction finds the digits. (3) `.PDF`/`.DOCX` (uppercase) bypassed the scanner — `SCAN_EXT.has(extname(p))` was case-sensitive. Fixed with a lowercasing `extOf()` used everywhere extension is checked, including inside `extractText`'s own dispatch. (4) DOCX headers and footers were invisible — `mammoth.extractRawText()` only reads the body. Fixed by also reading `word/header*.xml`, `word/footer*.xml`, `word/footnotes.xml`, `word/endnotes.xml` directly from the zip and stripping tags. (5) **The real risk DEF-37 was written for — contact info rendered as an image inside a `.docx`/`.pdf` — is structurally unfixable by text extraction; no library sees pixels.** Rather than leave that a silent pass, `extractText` now also reports `hasImages`, and `scan()` treats any PDF/DOCX carrying an embedded image as a hit requiring manual review — fail closed instead of fail silent. (6) `mammoth`/`pdf-parse`/`jszip` were added with caret ranges; pinned to exact versions per the DEF-17 convention | `node tests/no-pii.mjs --self-test` → `4/4 planted numbers caught`, plus 8 new checks (header, footer, image-flag ×4, uppercase-ext ×2), all `ok`. Mutation proof re-verified after the compression fix: stubbing `.pdf`/`.docx` to raw passthrough now correctly fails 5 of the 8 new checks and drops to `2/4` (previously it silently passed at `4/4`). Real-file proof, both directions: `resume.PDF` (uppercase, renamed from `Rohit_V3.pdf`) is now caught — phone number reported, where before the fix it scanned 0 hits. **`assets/inbox/resume/Rohit_V3.docx` and `FINAL-Rohit_Master_Resume.docx` — the two actual files DEF-37 was about — are now both caught** (`embedded-image` hit, exit 1), where round 1 reported them clean (their phone number is in `word/media/image1.png`, not in any XML part, confirmed by unzip + grep). CI: `npm ci` added to the PII job, verified locally by extracting a clean `git archive HEAD` checkout and confirming `node tests/no-pii.mjs --self-test` now finds `node_modules` present. **Round 3 (08-10) — two MEDIUM findings from round 2 that had blocked auto-merge, both fixed, same PR:** (7) **Fail-open on extraction errors.** `scan()`'s `catch` block on a thrown `extractText()` used to `console.error` and `continue`, so a corrupted or password-encrypted `.pdf`/`.docx` scanned as clean and the gate exited 0 — the one file that most needs a human's eyes was the one the gate said nothing about, and it contradicted the fail-closed rule this same commit had just applied to embedded images. Fixed: a throw on `.pdf`/`.docx` now pushes an `extraction-failed` hit and fails the gate closed, exactly like `embedded-image`. `scan` is now exported from `tests/no-pii.mjs` so the self-test can exercise it end-to-end against real (throwaway, uncommitted) temp files. (8) **`hasImages` blind spot on compressed PDF object streams.** The image detector was a raw-byte regex for `/Subtype /Image` over the whole file; a PDF 1.5+ document can put an image XObject dictionary inside a compressed object stream (`/ObjStm`), invisible to that regex — `hasImages` would silently read `false` with an image actually present. Chose the conservative fix over decompressing untrusted PDF internals: `extract-text.mjs` now also flags `cannotVerify` when the raw bytes contain an `/ObjStm` or `/Type /XRef` marker, and `scan()` treats `cannotVerify` the same as `hasImages` — a `compressed-object-stream` hit, fails the gate. `tests/lib/self-test-fixtures.mjs` grew past the 250-line module budget once these checks were added, so its PDF/DOCX byte-level builders were split out to the new `tests/lib/pdf-docx-fixtures.mjs` (150 lines) | Round 3 self-test: `node tests/no-pii.mjs --self-test` → still `4/4` planted numbers, now **15 checks total, all `ok`** (8 from round 1/2, plus 7 new: ObjStm-has-no-raw-image-signature, ObjStm-flagged-cannotVerify, plain-PDF-not-flagged, valid-files-still-pass-via-scan, corrupt-PDF-fails-closed-via-scan, corrupt-DOCX-fails-closed-via-scan, ObjStm-PDF-fails-closed-via-scan). **Mutation proof, fix (7):** reverted `scan()`'s catch block to the old `continue`-on-error behaviour — `corrupted PDF fails the gate closed` and `corrupted DOCX fails the gate closed` both flipped to `FAIL`, `SELF-TEST FAIL`; reverted back, `SELF-TEST PASS` restored. **Mutation proof, fix (8):** stubbed `cannotVerify` to always return `false` in `extract-text.mjs` — `ObjStm PDF flagged cannotVerify` and `ObjStm PDF fails the gate closed` both flipped to `FAIL`; reverted back, `SELF-TEST PASS` restored. Both mutations run against a real (uncompressed marker) `/ObjStm` PDF fixture and real truncated PDF/DOCX buffers, not synthetic assertions — proving each fix bites and the 18-check standard stayed intact throughout, not weakened to make it pass. **Round 4 (08-10) — one HIGH finding from round 3 review, fixed, same PR (#1):** (9) **Fail-closed only covered exceptions, not silent degradation.** Every prior round's "fail closed" logic (extraction-failed, embedded-image, compressed-object-stream) triggers only when `pdf-parse`/`mammoth` either throw or leave a raw-byte marker behind. Measured directly against `pdf-parse@2.4.5`: flipping a handful of interior bytes of an otherwise-valid `/FlateDecode` content stream (same length, so the `/Length` dictionary entry still matches) makes `pdf-parse` return `""` per page and report success — no throw, no marker, nothing to catch. Measured against `mammoth`: a technically well-formed, empty `<w:body></w:body>` makes `extractRawText()` return `""` with zero warning messages, also no throw. Either case previously produced `hasImages:false, cannotVerify:false`, an empty/near-empty `text`, and the file reported clean with zero PII hits — the exact fail-open pattern rounds 1–3 were supposed to close, one layer deeper, exactly as round 3's reviewer predicted. Fixed: `extractPdf` now computes meaningful characters per page (stripping `pdf-parse`'s own `"-- N of M --"` page-separator boilerplate first, so that can't pad a truly empty page into looking like content) and flags `cannotVerify` with `reason: 'sparse-text'` when that average falls under 15 chars/page; `extractDocx` does the same over total combined (body + header/footer/footnote/endnote) text, floor 15 chars. Both floors are deliberately low — chosen to bite reliably on total degradation (0 chars, what the libraries actually return per the measurement above) while sitting well under the self-test's own real fixture text (~25–35 chars), so there is no tension between catching degradation and not crying wolf; false positives above the floor are cheap (a human looks), false negatives below it are the thing this gate exists to prevent. `scan()` in `tests/no-pii.mjs` now branches on `reason` to report a `sparse-extraction` hit with its own message, distinct from `compressed-object-stream` | Round 4 self-test: `node tests/no-pii.mjs --self-test` → still `4/4` planted numbers, now **23 checks total, all `ok`** (15 from rounds 1–3, plus 8 new: degraded-stream-PDF-flagged, empty-body-DOCX-flagged, real-content-PDF-not-flagged ×1, real-content-DOCX-not-flagged ×1, plus the same four proved again end-to-end via `scan()` against real temp files). **Mutation proof:** set both `MIN_CHARS_PER_PDF_PAGE` and `MIN_DOCX_CHARS` to `0` (disabling the new check, simulating the pre-fix behaviour) — `degraded-stream PDF flagged cannotVerify`, `empty-body DOCX flagged cannotVerify`, and both corresponding `scan()`-level `sparse-extraction` checks all flipped to `FAIL`, `SELF-TEST FAIL`; restored, `SELF-TEST PASS` returned. The degraded-PDF fixture is a real same-length byte-flip inside a real `/FlateDecode` stream (not a synthetic assertion) and was independently confirmed against the installed `pdf-parse@2.4.5` to parse without throwing and return `""` per page — proving the mutation matches the real library's actual failure mode, not an assumption about it. Full gate suite re-run clean after the fix: build, both self-tests, both live scans, 64 Playwright specs (2 pre-existing skips, unrelated), boundary-check (worst seam 7.56:1), nav-contrast (worst 6.26:1). **Round 5 (08-10) — two HIGH findings from round 4 review (same underlying gap, reported twice), fixed, same PR (#1):** (10) **Round 4's sparse-text check was a WHOLE-DOCUMENT average, so one degraded page/part hid behind a healthy rest-of-document.** PDF: `meaningfulChars.length / pageCount` against the 15-char floor — a 2-page PDF with page 1 intact (dense real text) and page 2 silently degraded to `""` averages above the floor because page 1 alone clears it; `cannotVerify` stayed `false` and a phone number planted on page 2 was never recovered, never flagged, 0 hits. DOCX: body + every header/footer/footnote/endnote part were summed into one pooled count against the same floor — a healthy body masked a degraded footer the same way. Measured directly, not assumed: built a real 2-page PDF (page 2's `/FlateDecode` stream byte-flipped, same technique as round 4's `degradeStream`) and a real DOCX (footer XML reduced to an empty `<w:p></w:p>`), ran both through the actual `extractText()`/`scan()`, confirmed `cannotVerify:false` and 0 hits before the fix. Fixed: PDF now checks each page's own character count against the floor, splitting on `pdf-parse`'s own `"-- N of M --"` page separators (falls back to the old whole-document average only if the split doesn't yield the expected number of chunks); DOCX now checks the body and each present header/footer/footnote/endnote part against the floor independently. `cannotVerify` fires if ANY single page or part falls under 15 chars, not just the average. New fixture builders (`buildMultiPagePdf` in `pdf-docx-fixtures.mjs`; `emptyFooter` option on `buildDocx`) construct genuine multi-page/multi-part documents with independent per-page/per-part content, which the pre-round-5 fixture builders (single content stream only) could not do — the gap was invisible to the self-test by construction, exactly the same class of miss as the DEF-37-round-1 lesson two rows below. `tests/lib/self-test-fixtures.mjs` split its round-5 checks into the new `tests/lib/self-test-sparse-density.mjs` to stay under the 250-line module budget. **Round 6 (08-10) — owner's explicit decision: replace enumeration with structure, after 5 rounds of hole-by-hole patching each closed one place and the next round found a different one (PDF off-canvas text, PDF `/Annot` FreeText contents, DOCX `comments.xml`, DOCX footnote split-runs, DOCX drawingML text box, all reproduced with real constructed fixtures by round 5's reviewer, 0/5 caught).** Three structural changes, all three required: (11) **DOCX: stopped using a named-part allowlist.** `extractDocx` now also opens the `.docx` zip and reads EVERY `*.xml` entry inside it — `word/`, `docProps/`, anywhere — strips tags, and appends the result as a backstop, on top of (not instead of) mammoth's body extraction and the existing named header/footer/footnote/endnote reads. New `tests/lib/docx-archive-scan.mjs` (`scanArchiveXmlParts`), reusing the existing `stripXmlTags`. This is what catches `comments.xml` (never a named part) and a drawingML text box (`w:txbxContent`) living inside `document.xml` itself, which mammoth's `extractRawText` does not walk into. (12) **PDF: added a raw-decoded-stream pass, independent of page geometry.** `pdf-parse`'s `getText()` is rendered, visible-page text — measured directly: a `Tj` operator pushed off the `/MediaBox` via a large `Td` offset, in the SAME content stream as text `pdf-parse` does extract, is silently dropped, no error. New `tests/lib/pdf-raw-scan.mjs` (`extractPdfRawText`) decompresses every `/FlateDecode` stream found anywhere in the raw PDF bytes (this also extends the existing `/ObjStm` detection from flag-only to actually decoding it, since an ObjStm's dictionary is FlateDecode too) and scans the decoded bytes directly; it also extracts literal, uncompressed strings next to a `/Contents` key, since `/Annot /FreeText /Contents` is commonly stored that way and is never part of any page content stream at all. Runs as an addition to `pdf-parse`'s structured text, not a replacement — the existing per-page sparse-text check (round 5) still runs on the structured extraction only. (13) **Loosened the phone-number regex to survive XML-run splitting, without reopening "any digits near whitespace".** A DOCX footnote's phone number split across separate `<w:r>` runs turns tag-stripping's tag-to-space replacement into a run of several spaces at each seam (one space per stripped tag; a run boundary is at least 2 tags). Added a second rule, `phone-in-split`, alongside the unchanged original `phone-in`: it accepts a gap between digits ONLY if that gap is 2-4 whitespace/separator characters — never a single bare space. Measured directly why the single-space case had to stay excluded: an earlier draft that allowed 0-4 characters matched real SVG coordinate strings in `PrivateFigure.astro`/`QuorumFigure.astro` (`"76 102 54 130"` reads as 10 digits with single-space gaps) as false positives — tightening the floor from 0 to 2 cleared them while still catching the round-6 footnote fixture, whose stripped-tag gap is 4 spaces. New `tests/lib/round6-fixtures.mjs` builds all 5 reproduction fixtures; new `tests/lib/self-test-round6.mjs` runs the checks, wired into `self-test-fixtures.mjs`. Rounds 1-5's fail-closed backstops (embedded image, ObjStm/compressed-xref, extraction-throws, sparse/degraded text) are all still in place, unchanged — round 6 adds coverage on top, it does not remove the net | Round 5 self-test: `node tests/no-pii.mjs --self-test` → still `4/4` planted numbers, now **28 checks total, all `ok`** (23 from rounds 1–4, plus 5 new: 2-page-PDF-page-2-degraded-flagged, 2-page-PDF-both-real-not-flagged, DOCX-degraded-footer-flagged, DOCX-healthy-footer-not-flagged, plus the page-2-degraded case proved again end-to-end via `scan()` against a real temp file). **Written RED first, per TDD:** the new assertions were added before the fix and confirmed to fail exactly on the reviewer's two scenarios (`2-page PDF, only page 2 degraded, still flagged cannotVerify: FAIL`; `DOCX, healthy body but degraded footer, still flagged cannotVerify: FAIL`), then turned green by the per-page/per-part fix, nothing else. **Mutation proof:** reverted the PDF check back to `(meaningfulChars.length / pageCount) < MIN_CHARS_PER_PDF_PAGE` and the DOCX check back to summing body+parts into one pooled `meaningfulChars.length < MIN_DOCX_CHARS` — all 3 round-5 "still flagged" assertions flipped to `FAIL`, `SELF-TEST FAIL`; restored, `SELF-TEST PASS` returned. Full gate suite re-run after this fix, all green (see command block in the handoff). **Round 6 self-test:** `node tests/no-pii.mjs --self-test` → still `4/4` planted numbers, now **38 checks total, all `ok`** (28 from rounds 1–5, plus 10 new: the 5 fixtures each caught via direct `extractText()` + confirmed clean on the paired non-planted fixture, then each of the same 5 proved again end-to-end via `scan()` against a real temp file). **Written RED first, per TDD:** all 5 fixtures confirmed 0/5 caught against the pre-round-6 code (named-part allowlist, structured-only PDF extraction, contiguous-digit regex) before any fix landed. **Mutation proof, all 3 changes independently load-bearing:** (a) commented out the `scanArchiveXmlParts` call in `extractDocx` — `DOCX comments.xml caught` and `DOCX drawingML text box caught` both flipped to `FAIL`, the other 3 round-6 checks stayed `ok`; restored, `SELF-TEST PASS`. (b) commented out the `extractPdfRawText` call in `extractPdf` — `PDF off-canvas Td text caught` and `PDF /Annot FreeText Contents caught` both flipped to `FAIL`, the other 3 stayed `ok`; restored, `SELF-TEST PASS`. (c) reverted `phone-in-split` back to requiring contiguous digits — `DOCX footnote, digits split across runs, caught` flipped to `FAIL` alone, the other 4 stayed `ok`; restored, `SELF-TEST PASS`. **False-positive proof against real production content, not just fixtures:** `node tests/no-pii.mjs` (the live scan, not self-test) run against the actual repo both before and after the regex change — before the `{2,4}`-floor fix, the loosened `phone-in-split` draft matched real SVG coordinate strings in `src/components/figures/PrivateFigure.astro` and `QuorumFigure.astro` and in `dist/index.html` (their built output), 9 false-positive hits; after the fix, `✓ PII gate: 100 files scanned, no personal contact details found` — zero hits, confirmed clean both before this PR's changes and after. Full gate suite re-run clean after round 6: `npm ci`, `npm run build` (no new warnings), both self-tests (`no-pii.mjs`, `file-budget.mjs`), both live scans, 64 Playwright specs (2 pre-existing skips, unrelated to this change), `boundary-check.mjs` (worst seam 7.56:1, AA), `nav-contrast.mjs` (worst 6.26:1). New files stay within the 250-line/32,000-byte/120-char module budget: `tests/lib/docx-archive-scan.mjs` (40 lines), `tests/lib/pdf-raw-scan.mjs` (93 lines), `tests/lib/round6-fixtures.mjs` (148 lines), `tests/lib/self-test-round6.mjs` (104 lines); `tests/lib/pdf-docx-fixtures.mjs` (236) and `tests/lib/self-test-fixtures.mjs` (224) stayed under budget without needing a further split. **Round 7 (08-10) — one MEDIUM finding from PR-1 review, fixed, same PR (#1):** (14) **`phone-in-split`'s {2,4}-width gap alone did not distinguish a real split phone number from ordinary tabular content.** The round-6 fixture (a footnote's digits split across XML runs) and a 3-space-aligned table of single-digit columns (a plain-text table of scores or coordinates that happens to total 10 digits starting 6-9) both fall inside the same 2-4-character gap width — width was never the real signal. What differs is gap COUNT: the round-6 fixture splits into 4 multi-digit groups (`987`, `65`, `432`, `10`) joined by exactly 3 gaps, because Word/XML run boundaries are comparatively rare; a table split digit-by-digit needs a gap between every column — 9 gaps for 10 digits. Fixed: the `phone-in-split` rule gained a `filter` (checked in `scan()`'s match loop, and in both self-test `matches()` helpers) that rejects a match with more than 3 gaps. `tests/lib/self-test-fixtures.mjs` grew a `tableRow` fixture (`'7   1   2   3   4   5   6   7   8   9'`, 9 gaps, would have been a false positive) in its clean-text set | Round 7, written RED first per TDD: `tableRow` confirmed as a false positive against the pre-fix regex (`SELF-TEST FAIL`, false positive count > 0) before the `filter` was added. After the fix: `node tests/no-pii.mjs --self-test` → `SELF-TEST PASS`, all pre-existing checks still `ok` (the round-6 footnote fixture's 3-gap match still caught — filter threshold of ≤3 keeps it). Full gate suite re-run clean: `npm ci`, `npm run build`, both self-tests, `node tests/no-pii.mjs` (100 files, 0 hits), `file-budget.mjs` both modes, 64 Playwright specs (2 pre-existing skips), `boundary-check.mjs` (worst seam 7.56:1, AA), `nav-contrast.mjs` (worst 6.26:1). `tests/no-pii.mjs` (203 lines/9,784 bytes), `tests/lib/self-test-fixtures.mjs` (240 lines/13,134 bytes), `tests/lib/self-test-round6.mjs` (112 lines/6,521 bytes) — all within the 250-line/32,000-byte/120-char module budget. **Round 8 (08-10) — owner's decision: 7 rounds of content-scanning inside PDF/DOCX did not converge, so stop relying on it alone.** The gate now has two layers: (a) an unconditional block — `scan()` fails any tracked `.pdf`/`.docx` outright, without reading it, as a `pdf-docx-unconditional` hit for manual review; this, not the content scanner, is the real guarantee. (b) content scanning from rounds 1-7 still runs as best-effort early signal, with known gaps (hex-encoded PDF annotations, DOCX numeric character references, fixture-tuned thresholds) left open. A clean content scan is not proof of PII-free — only (a) plus a human review is. Zero blast radius today: no `.pdf`/`.docx` is currently tracked | Self-test: valid PDF/DOCX now assert exactly one `pdf-docx-unconditional` hit and no content-rule false positive — `SELF-TEST PASS`, rounds 1-7 checks still `ok`. Mutation both directions: planted a clean throwaway `.pdf`/`.docx` under `public/`, `node tests/no-pii.mjs` failed with `pdf-docx-unconditional` only; removed, gate clean again (104 files scanned) |

**Measured performance baseline — the site is already fast.** Lighthouse 13.4.1: desktop **100**
(LCP 0.4s, CLS 0.007, TBT 0ms), mobile **98** (LCP 2.4s, CLS 0.002, TBT 0ms). Scroll is locked at
60fps with **zero long tasks** on both. Do not "optimise" scroll, animation, or the blend overlay.

---

## Rejected — do not re-propose without new information

| Option | Why rejected |
|---|---|
| WordPress | $300/yr, wrong runtime for authored interactive graphics, and it argues against a reliability engineer's own positioning |
| DigitalOcean App Platform | 1 GiB transfer on the free static tier; $5/month per always-on container against Fly's $0.08 idle |
| Vercel | Hobby tier prohibits commercial use; Pro is $20/user/month |
| Migrating apps off Fly to Cloud Run | Saves pennies, costs a GCP project, Artifact Registry, IAM, and three migrations |
| Replacing the plate world entirely | Would rebuild six authored figures and the leader-line interaction to solve a page-structure problem |
| `phuryn/pm-skills` | 66 of 68 fail the how/where-not/what-not test. `positioning-ideas` would drag competitor-matrix framing into the copy |
| `andrej-karpathy-skills` | Not knowledge management — one 2.5 KB file duplicating the global `CLAUDE.md`. Claims MIT in frontmatter; **no LICENSE file exists** |
| `notebooklm-py` | Drives an unofficial Google API and stores a bearer credential on disk. Wrong shape for maintaining a repo's knowledge |
| `usage-guide` | Grade-2 reading level for end users operating a product. No such reader here |
| `security-and-hardening` | Five of six sections are server-side. This site has no server, no auth, no database |
| `VoltAgent`, `agentskills`, `vercel-labs`, `wednesday-solutions` | Two are not skills at all. The others are React/runtime-bound |
| Writing our own visual-review skill | `design-review` already exists, MIT, and is battle-tested |

---

## Corrections — mistakes made, kept so they are not repeated

| What I claimed | What was true | Lesson |
|---|---|---|
| DEF-37 round 1: "The PII gate can now see inside `.pdf` and `.docx`" | True for `.pdf` (both real files caught). **False for the two real `.docx` files DEF-37 was written about** — `Rohit_V3.docx` and `FINAL-Rohit_Master_Resume.docx` both scanned clean, because their phone number sits inside an embedded image (`word/media/image1.png`), which no text-extraction library can see. The self-test's 4/4-caught result only proved the plumbing worked for a number planted as literal text — it could not have caught this by construction | A self-test built from synthetic fixtures proves the code path, not the real-world case. When the defect names specific real files, run the fix against those files, not only against fixtures shaped like them |
| "CiteVyn uses Arize" | 27 matches were all the word **"summarize"** | A substring is not a token |
| "No file-size rule exists in NarraTwin" | It does — `ADR/0047:55`. I searched feature branches, not `main` | Read `main` |
| "You have no engineering skills" | `SKILL_LOCK.md` locks Addy Osmani, GitHub Spec Kit, and PM Skills | Scope a claim to what was actually checked |
| "Fonts are 76% of the payload" | Fonts are `unicode-range` subset; real cost ~91K. Two PNGs are 60% | Measure what is fetched, not what exists |
| "ui-ux-pro-max has independent heuristics" | 115 of its 208 rules cite the same WCAG/HIG standards impeccable uses | Independently compiled is not independently derived |
| "Going public needs one reworded line" | Four lines across three files; `PRODUCT.md` was worse | Scan before asserting scope |
| The architect's proof that `.js-ground` fails open | The probe did not isolate the variable — at scroll 0 the **healthy** page also shows 14 nodes at 1.03:1, because `#private` was never scrolled into view. Conclusion right, evidence invalid | A right answer from a wrong proof is still a wrong proof |
| Security: "LICENSE and RCA-002 are untracked" | Both present in `git archive HEAD`. Two false claims, each killable by one command | Widest brief, lowest floor — verify every individual claim |
| "`project-doc-skills` has dangling references" | It does not. The earlier review read `skills/<name>/`, a build **input**. The product is `dist/*.skill`, a 66 KB zip with all 16 files | Test the artifact that ships, not the source that builds it |
| "`project-doc-skills` is a poor fit" | `architecture-and-decisions` is now the **second-best** install of everything evaluated | A verdict inherited from one agent is a claim, not a finding |
| SaafSaans "started at Build with AI as a three-hour build" | The repo records no duration anywhere. The first commit of the day is "existing UI baseline", and the case study dates the Streamlit app to before 18 July | An owner-stated figure the design doc itself said to label went to the site unlabelled. The rule was written and then not applied in the same change |
| "The certificate is valid and the address resolves" | The address resolves; the server presents no certificate at all | A sentence with two clauses needs two checks. Half a verified claim is an unverified claim |
| A commit message saying "nothing said twice" | Relocation was stated in two places, in two wordings | Claiming an absence needs a search for it, not a memory of intending it |
| `tests/contact.spec.js` "prevents the contact plate being deleted" | Deleting the entire plate passed. So did hiding the whole page | A gate written after its fix has never been observed failing. Watch it fail or it proves nothing |
| "The buildathon's first slot was 16:45, so the 18 July commits precede the event" | **There is no 16:45 slot.** The event ran 09:00–17:30 IST (owner-confirmed, 2026-08-08). The commits at 14:17, 14:49 and 15:21 are *inside* it. I relayed a reviewer's figure into the brief of a six-agent workflow without checking it, and it reached this ledger twice | An unverified figure put into a shared brief is not one error, it is one per agent. Check before you brief |
| Five review passes read the footer and none read its words | The critique counted the colophon at 123 characters and 10.56px and never asked what it said. "work sample" appears **0 times** across the entire critique. Two review agents and a cross-model pass also missed it | A measurement is not a reading. A reviewer told to check typography checks typography; nobody was asked whether the sentence was worth saying |
| `AGENTS.md`: "Cloudflare Pages auto-deploys every push to `main`. There is no gate between a push and production" — the standing debt's opening sentence, repeated in D14 and D15 | **The project has no git integration at all.** `wrangler pages project list` → `Git Provider: No`. Nothing has ever deployed on push; every deploy is a manual CLI run. The stated risk was the opposite of the real one: there *is* a gate, and the real problem is silent drift — production sat six commits behind `main` and nothing said so | A deployment model recorded in prose and never checked against the platform. One command settles it, and the sentence had been repeated in three places for two days without anyone running it |
| Gates run immediately after a deploy go red and then pass | **Three occurrences on 09 Aug, all transient, none reproducible.** (1) the seam gate reported `1.00:1 at "overview → citevyn"`, clean on the next three runs; (2) `/cv` returned **404** twice while its own content was being served, correct within a minute; (3) the nav gate reported a failure, clean on the immediate re-run at 15.60:1. **Cause UNVERIFIED** — CDN propagation is the leading hypothesis and it is not proven; a half-propagated deploy serving new HTML against old CSS would produce exactly this. **This is not a curiosity, it is a blocker for approach C**: the standing debt's target is deployment through CI with gates, and a post-deploy gate that goes red one run in three is worse than no gate — it trains you to re-run until green, which is how a real failure gets waved through. Any post-deploy check needs a settle wait and an N-of-M rule stated up front, NOT a bare retry | Wait, measure, measure again. And never let a gate's remedy be "run it until it passes" |
| `docs/positioning-phase0.md`: the method was "`brand-positioning`, paired with `grilling`" | **`grilling` was never invoked.** The adversarial pass was done inline and then described as the skill. The owner caught it by asking whether appropriate skills had been used | A claim about *method* is a claim, and it needs the same check as a claim about a number. Writing it into a committed document — on a site whose subject is claims that survive a check — is the worst place to be loose. Say what was run |
| `/cv` was built as "the CV work (D31, approach B)" | **None of the five ResumeSkills was invoked** — `tech-resume-optimizer`, `resume-quantifier`, `executive-resume-writer`, `portfolio-case-study-writer`, `job-description-analyzer`. D35 installed them for exactly this task, one session earlier, after a two-round search. The page was built from first principles instead | Installing a skill is not using it. The register that records WHICH skill a task needs should be checked at the start of that task, not at the end of the session |
| `docs/positioning-phase0.md`: *"Everything else in this file is applied"* | **Two of its four Changed rows were not.** The headline still shipped D23's rejected truncation, and the contact plate never received the owner's sentence — while the five-title problem it claimed to fix had merely relocated there. Found by `/impeccable critique` | **Second** unchecked claim in the same file, after the `grilling` provenance line. Both are a planning document asserting its own execution. A doc that says "applied" needs the same gate as a number that says 52 |
| Oracle's numbers and `paint-grain.png` are the biggest asset problem | `dist/og.png` is **337,549 bytes** — twice `paint-grain.png` at 166,280, and the largest file in the build. I recorded paint-grain as the biggest and never listed the build by size | Rank by measurement, not by the asset you happened to be looking at |
| The workflow judge's S1: the event page forbids pre-built work | Two fetches of that page returned two different schedules, and neither contained the quoted rules. The finding was never reproducible | The layer built to refute the others needs refuting too. A quote is evidence only if a second fetch returns it |
| **"`FINAL-Rohit_Master_Resume.md` CONTAINS A PHONE NUMBER"** — stated in D37, D38, D39, `assets/inbox/README.md` and the handoff | **It does not.** The `.md` contact block carries e-mail, LinkedIn and Visa only, and the whole file contains **zero** digit-runs of eight or more. The number is in **`Rohit_V3.pdf` and `FINAL-Rohit_Master_Resume.pdf`** — the contact line, beside the e-mail. `textutil` renders neither `.docx` to a phone number, so the plain-text extractions are clean too. **The conclusion was right and the location was wrong**, which is the dangerous combination: the ledger pointed the next reader at the one resume file that is safe, and away from the two that are not. It also hid DEF-37 — a `.md` is scanned by the gate, a `.pdf` is not | Naming the wrong file is not a smaller error than missing the risk. Locate PII by running the scanner's rules over every candidate, not by reading the one file that is easiest to open |
| Oracle "MTTD reduced ~35%, release cycle time reduced ~25%" — `PRODUCT.md:160`, inherited by D25, `ACTION-PLAN.md:86` and `positioning-decisions.md:64` | The authoritative CV **never says MTTD.** It says *"reducing root-cause analysis time by 35%"* (time to **diagnose**) and *"reducing release validation time by 25%"* (a **component** of cycle time). Both restatements are larger than their source | A metric renamed is a metric overclaimed. MTTD and RCA-time are different numbers; so are validation time and cycle time. This was a paraphrase that four documents then treated as the fact |
| The handoff: "Do NOT add to src/styles/global.css. It is at 498 of a grandfathered 500" | **It was at 505.** The ceiling was already breached when the warning was written; the warning itself was the evidence nobody had run `wc -l`. The breach is DEF-47 and the ceiling is now a CI gate | A limit stated in prose is a limit nobody has measured lately. Numbers in a handoff need the same check as numbers on the site |
| The ledger's DEF-14 mutation: "an empty link `href=\"/x\"` planted in the nav went undetected" | Planted verbatim it IS detected — by `links.spec.js`, as a 404, before axe ever runs. Red for the wrong reason: the a11y hole was real but the recorded mutation could no longer isolate it once the resolve gate existed. A resolvable `href="/cv"` shows the true hole | A recorded mutation ages as other gates land. Re-run it before trusting it, and record WHAT it fails on, not only THAT it fails |
| DEF-17: "One `npm install` moves the Chromium binary and invalidates every future baseline" | CI runs `npm ci` from a lockfile that pinned all four packages, and zero screenshot baselines exist to invalidate. The caret was a real local-drift risk and is now pinned; the stated blast radius described infrastructure this repo does not have | Sizing a defect means checking what it can actually reach, not what the same defect could reach somewhere else |
| The CV is the source of truth for project numbers | It is stale on CiteVyn: `Rohit_V3` says *"361+ automated tests and a 50/50 golden evaluation suite"*. DEF-31 counted **1,036 test functions across 110 files** and **52** golden cases at `df8cfc3`. **The site is right and the CV is wrong** | D36 makes `Rohit_V3` authoritative over the *other resume*, not over a count run against the repo. Authority is scoped to the conflict it was written to settle |
